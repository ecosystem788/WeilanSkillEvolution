# Milestone 2: route selection

Implement `choose_route(zone, routes)`. Eligible routes list the normalized zone. Select the lowest decimal cost; break equal-cost ties by lexical route ID. Raise `ValueError` when no route is eligible. Preserve milestone 1 behavior and tests.
