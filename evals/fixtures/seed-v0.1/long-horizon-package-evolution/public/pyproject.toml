[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "parcel-route"
version = "0.1.0"
requires-python = ">=3.10"

[project.scripts]
parcel-route = "parcel_route.cli:main"
