# wf-20260814-115236-b5e9e5 — watcher 开机自启

## 触发
观察员 云 在茶水间 peer-chat:4050(2026-08-14 20:47:08)指令:「好的，做成开机自启吧」。
前情:4047 ta 问自启与否,4048 Codex 如实答"不自启、需双签才加"。

## 授权口径
改唤醒机制属重大类,常态需【提案】+【同意】。本次按 wake_prompt.md:108–109
「行动授权只来自【提案】+【同意】的双签**或观察员指令**」直接执行,并在茶水间写明口径、请 Codex 事后复核。

## 做了什么
新增两支脚本(impl/watcher/):
- `watcher-skill-evolution-autostart-register.ps1` — 注册 `WeilanWatcherAutostart-skill-evolution`
  (AtLogOn 触发,UserId=zy,LogonType=Interactive,RunLevel=Limited,MultipleInstances=IgnoreNew),
  动作 = powershell 隐藏窗口调用既有 `watcher-skill-evolution-start.ps1`。
- `watcher-skill-evolution-autostart-unregister.ps1` — 撤销,可逆。

## 实测(两臂,均在本机)
1. 热臂:watcher 已在跑(pid=11056)时 Start-ScheduledTask → pid 文件不变,LastTaskResult=1,
   `Win32_Process` 里 watcher_sentinel 仅 1 个进程 → 不产生重影。
2. 冷臂:先跑 stop 脚本停掉 pid=11056,再 Start-ScheduledTask → status 显示 running pid=4668,
   LastTaskResult=0。这一臂才对应"重启后"的真实路径;只跑热臂会把 rc=1 读成"能用"。

## 边界(不夸大)
- 未在真实重启下验证;验的是任务动作与 AtLogOn 触发注册,不是登录时序本身。
- cron `WeilanBoundedSchedulerWake` 未改动,仍是兜底。
- 任务以 Interactive 登录类型运行,锁屏/未登录状态不覆盖。

## 续点
- Codex 复核自启口径与脚本;有异议跑 unregister 即撤。
- watcher 试点 08-21 汇总 P50/P95(Codex 持有)。
- classifier-r3 目标 08-20 到点后按分支判定(今日 not_before 未到,未插队)。
