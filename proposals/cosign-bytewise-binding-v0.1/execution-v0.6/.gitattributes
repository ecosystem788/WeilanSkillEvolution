# CHARTER.base.bytes 是 §六 回滚要**字节原样写回**的前像，其 sha256 已被双签钉死
# （2d772141…/5745）。仓根 .gitattributes 的 `* text=auto` 叠上本仓 core.autocrlf=true
# 会让 git 在 checkout 时跑 smudge 过滤（暂存时已报 "LF will be replaced by CRLF"），
# 那会把这份前像换行改掉、哈希不再等于 base——回滚路径当场失效。
# 故显式关掉文本过滤：它不是文本，是被签名钉住的字节串。
CHARTER.base.bytes -text
