# v0.2 隔离候选评审 — method-state atomic replace retry（廉价 80% 修）

**评审人**：Claude（spec/评审/审计梯度）
**时间**：2026-07-11 00:56（UTC+9）
**委派来源**：codex-inbox `27e46d9719ee`（Claude 00:44:02）→ 提案 `POST_DEPLOY_FINDING-20260711.md` 的廉价修(1)
**候选路径**：`proposals/method-state-atomic-replace-retry-v0.2/candidate/solve-with-weilan/`

## 结论：通过。可逆候选完整、正确、绿、范围干净，已具备呈交项目方红键采纳的条件。

## 一、范围核验（对部署基线 diff）

`diff C:/Users/zy/.claude/skills/.../runtime_core.py  <候选>/runtime_core.py`

**唯一改动 = `replace_with_retry`。** 部署树的其余部分、wake_codex.ps1、wake_prompt、真账本均未触碰。签名向后兼容（只放宽默认值，调用点无需改）。

| | 部署基线 v0.1 | v0.2 候选 |
|---|---|---|
| 参数 | `attempts=5, delay_s=0.1` | `attempts=8, delay_s=0.05, max_delay_s=0.4, backoff=2.0` |
| 等待形态 | 固定 5×0.1s = **0.5s 定长** | 指数退避 0.05→0.1→0.2→0.4→0.4… **总预算 ~2.35s** |
| 耗尽路径 | 静默 raise | raise 前向 **stderr 打一条 JSON 计数**（`replace_with_retry_exhausted`：attempts / permission_errors / total_wait_s / exhausted / target） |

这正是提案要的两件事：(a) 把总预算从 0.5s 拉到 ~2-3s 盖住并发持锁窗；(b) 把"凭猜"换成耗尽路径的"凭数"。

## 二、测试核验（Claude 侧独立复跑）

- targeted `test_event_atomic_replace_retry.py`：**3 passed in 1.98s**
- 候选全回归 `pytest -q`：**60 passed in 36.99s**

三个新测试覆盖到位且断言精确：
1. **盖过旧 0.5s 预算**：模拟 0.75s 持锁窗，第 5 次成功、总等待 0.75s、frame 完整、无残 tmp。证明新预算确实覆盖了超过旧 0.5s 的现实竞争窗。
2. **耗尽仍安全**：全程 PermissionError → 8 次尝试、sleeps=[0.05,0.1,0.2,0.4,0.4,0.4,0.4,0.4]、total_wait=2.35s、**既有 frame 原样保留、无半截账本**、耗尽计数记录数值正确。
3. **并发写不撕裂**：8 线程写 40 帧同时另一线程扫目录，无扫描错误、40 帧齐全、无残 tmp。

模拟竞争窗时长：测试用 monkeypatch 把 `time.sleep` 换成累加器、用一个 elapsed 阈值（0.75s）当"持锁窗"，因此断言的是**退避序列的形状与总预算**，不是真实墙钟——形状对、预算 2.35s > 现实观测到的 ~0.5s+ 窗口即为覆盖证据。真实竞争窗的墙钟长度仍需部署后 canary 用耗尽计数日志坐实（见下）。

## 三、遗留边界（不在本轮，需红键）

- **durable 根治仍是二期红区**：让 frame 读者带 `FILE_SHARE_DELETE` 打开（projection-rebuild / memory-recall / 并发 wake 的默认 `open()` 在 Windows 下不带 share-delete，才是挡住 `os.replace` 的真因）。本轮是廉价 80% 缓解，不是根治。
- **采纳/部署到线上 runtime_core（两处部署树）= 项目方红键**。评审只确认候选就绪，不跨采纳。

## 四、一处协调缺口（需 Codex 对账）

候选代码已交付且完整，但 `codex-inbox-replies.jsonl` **无对 `27e46d9719ee` 的回执**、`codex-inbox-processed.jsonl` **无该 id 的 processed 标记**。Codex 昨夜整晚 method-state 账本写入被 WinError 5 挡（正是本 bug 本身），回执写入疑似同批丢失——委派看似 open，实则代码已 done。请 Codex 下次醒来对账：补一条回执 + processed 标记，注明其自测原文，以便账本闭环。
