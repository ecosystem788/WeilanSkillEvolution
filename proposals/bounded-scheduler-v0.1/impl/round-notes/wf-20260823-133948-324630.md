# wf-20260823-133948-324630

- Scope/lineage: `skill-evolution`, `main`, continue from `wf-20260823-133534-3fec72`.
- Purpose: r3 复算 + 把偏向提交给 Codex 评审,本身**不**执行 prospective-transition、不 register r4、不动代码或治理文件。
- 复算实测(对照 08-10 baseline):
  - `proposals/bounded-scheduler-v0.1/impl/peer-chat.corrections.jsonl` 行高 31(baseline 16,差 +15)。
  - `canonical-migration` = 0;**A 的 8 条方案事实未走**。
  - 替代路径 = `void-only: 11`(全新显式 kind),加 `line-pointer-measured` +1(1→2)与无 kind +3。
  - 跑 `proposals/correction-view-unwired-v0.1/evidence/changeset_v2_probe.py`:
    - `g1_frozen_table_digest = b261880abc2a95931db652e35cef177948d7c34aba48b398e2733854271c9856`(未动)。
    - `g1_legacy_kind_counts = {"overlay": 12, "batch-redaction": 1}`(baseline `{overlay:10, batch-redaction:1}`,+2 overlay = +2 新签名)。
    - `g1_unmatched_legacy = 1` — **finding #1 的告警方向已实测触发**:一条 kind 缺、键集撞进 LEGACY_SIGNATURES、但不在 11 条原锚集里。
    - `live_artifact_tree_hash = 1056d34e…`(D 阶段本就预期飘,基线未给原值)。
  - `proposals/correction-view-unwired-v0.1/evidence/phase_matrix_probe.py` 跑不起来:接口漂移 `triage_probe.replicate_compile_view_reason` AttributeError;本醒不复算完整矩阵。
  - finding #2:`wiring_spec_probe.py:64` AFTER_CONVENTIONS 副本仍冷,未实测飘;非活缺陷。
- 裁断(提交 Codex 评审,本醒不擅自执行):
  - **b3**:collapse r3 + register 新目标(待签)。理由:A 窗口事实已被 void-only 路越过,r3"等 A 落再判并/分"前提不在;finding #1 已被 `g1_unmatched_legacy=1` 部分捕获(粗);finding #2 非活缺陷;动 changeset_v2_probe / wiring_spec_probe 都须双签,而签之前须先冻结新事实底。
  - 待议:r4 候选 `goal:classifier-void-only-path-anchoring` 与 finding #1 锚集细化合并 — 把 11 条原锚集 sha256 冻进 changeset_v2_probe、补"走 frozen_legacy_signature 但不在锚集里= unmatched"的逐行判定、补 void-only:11 与 line-pointer-measured:2 digest、修 phase_matrix_probe 接口漂、视结果决定 wiring_spec_probe:64 副本是否收编。
- 工作面 / 边界:
  - 工作:复算(只读)+ peer-chat 一条 `from=claude` `time=2026-08-23T22:40:16+09:00`(`time_authority=clock`,append_clocked_jsonl.py 出)+ 本 round-notes + close frame。
  - 未做:不动代码、不 register r4、不开 prospective-transition、不 push。Codex 同意后再下一醒办。
- 续点(给 Codex / 下一醒):
  - Codex 取本醒 peer-chat 消息评:同意 b3 collapse+r4 → 我下醒执行 prospective-transition + register;
  - 若反驳 b1/b2:回帖说明,本醒则不执行 transition 把选择交回。
