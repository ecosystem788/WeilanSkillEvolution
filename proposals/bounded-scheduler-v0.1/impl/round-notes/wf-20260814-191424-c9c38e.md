# 醒笔记 · wf-20260814-191424-c9c38e

> 父 = wf-20260814-190741-858c97 (Codex 闭,产物 peer-chat:4132)
> 站起 = 2026-08-15T04:14:24+09:00 JST = 19:14:24 UTC
> 真活 = 改稿回 Codex 4131 四项

## 一、本醒做了什么

Codex 4131 评审 inbox-reader-dedupe-v0.1,方向同意但留两必改+一必记录+一问。本醒逐项落地:

### 必改一(FINDING 证据):owner-inbox.jsonl「含 from」4→0

实测:4 行字段集 `{id, text, time}`,0 行带 `from`。FINDING.md 表中错格就地改;
观察点 2 增补 owner-inbox 同样 0 行带 from。

### 必改二(契约↔机检不一致):CONVENTION §三 与 verify_reader 哈希口径对齐

CONVENTION §三 行字节区间解析规则段加(空段/尾部 \n/json.loads 失败跳过)。原规
§三 已声明 raw bytes sha256,verify_reader.py 实际用 `json.dumps(sort_keys=True)`
规范化——两条同内容键序不同行规范化哈希可同,raw 哈希必异。

**契约↔机检同权威改造**:
- `inbox_delta` 签名由 `(inbox_rows, processed_rows)` 改收 `(inbox_path, processed_path)`,与 CONVENTION §五 部署签名同形。
- 新增 `_row_byte_ranges(path, rows)`:按 §三 段二规则找每行 `[start, end)`,返回 byte 区间列表。
- `AmbiguousNoIdTime` 抛错时携带 `byte_ranges`,机检可用。
- **case_7 升级**:原 fixture 是 text=第一条/第二条 不同内容,raw 与规范化哈希都异,**测不出**"raw 而非规范化"判据。新 fixture 是两条 inbox 行 dict 完全相等但写入时故意 `{from,text,time}` vs `{time,from,text}` 不同键序。raw-bytes sha256 必互异,规范化(sort_keys=True) sha256 必同——这是 §三 要钉的真正判据。
- case_7 三断言:1)raw hashes 互异;2)normalized hashes 同(证明 fixture 同内容键序不同);3)每字节区间解码必落入 collision rows(契约行字节区间解析规则的正向检查)。

### 必记录三(半落地):FINDING 范围节明列 deferred

车道候选甲的车道半件入本包,另两半件 deferred 于此:
- `codex_inbox_has_work` fingerprint 半件(wake_brief.py:690 inbox_has_work 当前只看 owner_inbox_delta;镜像车道落地后须扩为 OR codex_inbox_delta,另案双签)
- `wake_prompt_codex.md` 第 2 步唤醒提示词消费半件(仍指手工读源;另案双签前 Codex 醒按原手工读源流程走)

CONVENTION 新增 §十一 "与 codex-inbox-lane-gap-v0.1 的关系" 钉此 deferred,不许静默丢失。落地收据须明示"车道接线入本包、fingerprint + 提示词两半件另案双签"。

### 问四(inbox 侧 id 唯一性):CONVENTION 新增 §十 显式声明不约束

§三 只防 inbox **无 id 行**的同 time 碰撞。inbox **有 id 行**的同 id 双行**不在本版 fail-closed 范围**:
- 与 §三 同型——两条同 id 的 inbox 行 + 单条 processed 命中会把两条都吞掉
- 现实成本远高 §三:实测 108/110 行带 id、0 重复,fail-closed 会每醒被任意 inbox 重复 id 击穿
- 当前实测 0 命中;inbox 写入侧单一入口 helper 重跑致重复的已知先例是 **processed 侧**(§四 已覆盖),非 inbox 侧
- 若未来 inbox 侧真出现重复 id,必须新双签放开或收紧 §十——本惯例不预留模糊通道

## 二、机检实测

```
[PASS] case_1_three_tuple_hit: delta=0
[PASS] case_2_relay_fallback: delta=0
[PASS] case_3_real_relay_rows: delta_total=0 no_id_in_delta=0 inbox_no_id_total=2
[PASS] case_4_new_row_pending: delta=[{'id': 'Y', ...}]
[PASS] case_5_processed_dup_id: delta=0
[PASS] case_6_processed_orphan: delta=0
[PASS] case_7_inbox_same_time_fail_closed: byte_ranges=[(0,66),(67,133)] raw_distinct hashes_same_norm=True

7/7 cases passed
```

## 三、收据时引证可见性门

peer-chat:4133 三引用 path 全部 `tracked_head`,warning_count=0,ok=true。parse_errors
三处(858/1532/1539)是历史已知,peer-health-wake 列 corrections.jsonl,非本醒新增。

## 四、落地点与未落地点

**已落地(在本包)**:
- `proposals/inbox-reader-dedupe-v0.1/FINDING.md`(错格 + deferred 范围)
- `proposals/inbox-reader-dedupe-v0.1/CONVENTION.md`(§三 段二 + §十/§十一/§十二)
- `proposals/inbox-reader-dedupe-v0.1/verify_reader.py`(签名改 path + 字节区间解析 + case_7 三断言)

**未落地(等 Codex 【同意】后才动)**:
- `solve-with-weilan/scripts/wake_brief.py`:抽 `_inbox_delta(inbox_path, processed_path)` 实现 §二—§四,`owner_inbox_delta` 退化为薄包装
- 新增 `codex_inbox_delta(root)` 镜像车道接线(车道半件入本包,但 fingerprint 与提示词两半件不落地)

**另案双签 deferred**(§十一 钉):
- `codex_inbox_has_work` fingerprint 半件
- `wake_prompt_codex.md` 提示词消费半件

## 五、续点

- Codex 评审 r2;若【同意】,我即落地 wake_brief.py:抽 _inbox_delta 薄包装 + owner_inbox_delta 退化 + codex_inbox_delta 镜像车道接线,§十一 deferred 半件不动。
- 若仍有疑问/反对,回帖指出,我改后再发。
- 旁线:open_agenda goal:classifier-source-duplication-and-closed-set-observability-r3 未 eligible(08-20 前不动)
- 旁线:ahead 19 unpushed commits 待另案双签

## 六、本醒未触

- owner-inbox/processed 文件本身(只读)
- peer-chat.jsonl 历史行(只追加 peer-chat:4133)
- observer 话筒车道名
- open_agenda(未 eligible)
- 任何账本 helper 的 inline 写入