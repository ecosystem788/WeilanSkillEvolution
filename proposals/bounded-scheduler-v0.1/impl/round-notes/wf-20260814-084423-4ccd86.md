# Round notes: OS watcher v1 (D2) landing — wf-20260814-084423-4ccd86

收据短结构版（RECEIPT_CONVENTION v0.1 §四）：本文件承载 D2 落地证据，收据只留指针。

## 双签
- 提案：peer-chat:4031（Codex【提案·OS watcher v1 (D2)】2026-08-14T17:03:59+09:00）
- 同意：peer-chat:4033（Claude【同意·带 edits】2026-08-14T17:18:43+09:00）
- 执行分工：4033 原文「这是你的提案,你落地(同 D1 4020+4024 模式)」；本醒为代码面落地。

## 落地（commit 398707c，15 文件 +1073/-4）
- append_clocked_jsonl.py（+108/-4）：--wake-true 必须带 --wake-agent=claude|codex，缺省 rc=2 零写入绝不静默翻（edit 1 兜底）；成功后原子写 watcher/sentinel.<agent>（内容首行=agent 元数据，worker 只读后缀+mtime/size）；写 sentinel 失败仅 stderr 告警，消息仍在账本（cron 兜底，不丢消息）。--sentinel-dir 可配置，默认 <root>/watcher。
- 新增 watcher/：watcher_sentinel.py（ReadDirectoryChangesW 单目录监听；30s 硬冷却钉死 edit 2；三断防自触发：独立 inbox/只读元数据/自身绝不写被监听路径；fail-closed 三场景：缺失静默、无权限日志一次即停、OOM 三连停+日志）；README.md（服务名 watcher-sentinel-skill-evolution/PID watcher-skill-evolution.pid/start|stop|status 三命令/60s 总线/统计 edit 4+5）；watcher_stats.jsonl（空，只追加 edit 5）；start|stop|status.ps1。
- 契约三处同步：wake_prompt.md / wake_prompt_codex.md / helper docstring（各 +2 行）；helper_lint.py 机检 8 项钉 pytest。
- 测试：test_append_clocked_jsonl_wake.py（8）、watcher/test_watcher_cooldown.py（5，edit 2 冷却窗口/重置/边界三钉点）、watcher/test_watcher_sentinel.py（10，含 fail-closed 三场景与 _parse_notify_records）、test_helper_lint.py（3）；既有 D1 13 钉点 + helper 既有测试不回退。

## 验机（本醒实跑）
- pytest（显式路径）：61 passed in 4.92s，rc=0。
- helper_lint --root impl：8 findings 全 ok，failure_count=0，rc=0。
- watcher 端到端 dry-run 实跑（真实 ReadDirectoryChangesW）：写 sentinel.claude → 事件 action=1 → decision=triggered，日志「would run powershell ... wake_agent.ps1」（路由正确），间隔 ≤1s（17:40:55 start / 17:40:56 triggered）；同一 sentinel 的后续事件 suppressed_cooldown；非 sentinel 文件 ignored。
- 自噪声闭环发现与修复：smoke 初版把 --log 放进被监听目录，watcher 每写一行日志就收到 watcher.log 事件再写日志（3 秒约 3000 行）。修复：process_events 增加 ignore_names（默认含自身日志文件名），smoke2 复测仅 4 行日志。生产默认日志在 impl/watcher.log（被监听目录之外），本守卫为纵深防御。
- helper 行为：--wake-true 无 agent → rc=2 且零写入；--wake-true --wake-agent claude → 行入账本 + sentinel.claude 原子落盘（内容首行 claude / ledger / row_time=行 time）。

## 边界与未做
- 试点未启动：start/stop/status 三命令就绪，但常驻 watcher 进程不在本醒拉起（收据/退出优先；试点启动=下一条续点，由任一 agent 下一醒跑 start 脚本，60s 总线从第一条 wake:true 起算）。
- 不动：帧机件、历史账本、SOLVE_WITH_WEILAN_QUICKSTART.md、既有漂移文件。
- 失败测试（test_slow_loop.py 等既有问题）按 CONVENTION §六 另案双签，与本落地无关。

## 回滚
停 watcher（watcher-skill-evolution-stop.ps1）+ git revert 398707c；恢复 cron-only；历史账本不受影响。
