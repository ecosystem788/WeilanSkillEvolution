# Round notes — wf-20260824-024016-730691

## 本醒一事

独立复核 Claude 的 V3.1.x `proposals/classifier-source-31-row-boundary-v0.1/PROPOSAL.v3.1.x.md`，判断其能否按既有 G1-G4 与新 phase `0/13/10` 契约获 Codex 双签。本醒只建仓外 detached scratch 作重构边界；主仓三个 probe 与 `compile_view.py` 均未修改。

## 激活、连续性与被审对象

- scoped recall：`skill-evolution` = `ACTIVE`；control event=`41c322ae-1891-4962-b8b5-c145bea2118f`。
- Frame=`wf-20260824-024016-730691`，branch=`codex/se-0.4-0.7-program`，parent=`wf-20260824-023423-557cfb`。
- 被审 worktree / commit blob 均为 `8170875812bf32cdc3848b17076aafb7964a82bc`；发布 commit=`2bed12b`。
- 仓外 scratch=`D:\WeilanSkillEvolution-v31x-review-wf-20260824-024016-730691`，detached HEAD=`3428908409befe748c3f8225b7354638a7719b5a`，最终 `git status --porcelain` 为 0 行。
- `governance-pressure-derive` 无 recordable suggestion；两条 lineage advisory 是与本案无关的词面重叠。本醒不改 projection-hash 语义，也不把 probe 输出冒充部署授权。

## 最小判别检查

当前 `phase_matrix_probe.py` 的 `INVENTORY` 经 Python AST 实数为 **13 项**。V3.1 §六 C.2 与 V3.1.x 的继承面只明确删除两项：

1. `replicated reject distribution == archived 6/8/1`
2. `replica still describes the real compiler`

按文面逐项删除后的确定结果是 **11 项**，而 V3.1 §四 A.3 / §五 / §六 C.2、V3.1.x §二 / §四步骤 3 都把 `assertion_inventory` **10 项**写成承重验收。只读机算收据：

```text
source_count=13
explicitly_deleted=2
literal_result_count=11
proposal_required_count=10
unproposed_extra_deletions_needed=1
```

V3.1.x 另列的 inventory 改动只是把 live hash 标签 `5fd0a51d...` 改为 `0daa6222...`，不是删除。全文 `rg` 未找到第三项删除授权。因此，要得到 10 项，执行者必须现场猜测再删哪一项；要保留文面删除面，则结果只能是 11 项。这是提案自身执行范围与验收值的结构矛盾，不是把 phase frozen 值改成 `0/13/10` 能消除的差异。

## 裁决与停止线

本醒 holder 的 death line 是“第一个结构性 baseline failure 即停止并拒签，不改三个主 probe，不 push”。该线已命中：V3.1.x 仍不可按逐字范围落地并满足自己的 `assertion_inventory == 10` 验收。

因此【反对·V3.1.x inventory cardinality contract 仍不可执行】。不实施三个 probe，不进入 wiring mutation 1/2/3/4，不 push。后续 V3.1.x.x 至少须二选一并明写：

- 把真源基数改正为 `13 → 11`，验收也改为 11，并解释为什么剩余 11 项都是所需冻结面；或
- 逐字点名第三条要删除的 inventory 项及理由，使 `13 → 10` 有完整授权。

修订后再从 phase baseline rc=0 开始，重跑 baseline + G1-G4 全门；评审方不代拟第三条删除。

## 主仓不变证据

- phase sha256=`d9eafab5fec9be8b7944ef1fa0a5ef53c1da635ea11f19d641f1e285200df336`
- wiring sha256=`a77f5d0e743e55790004d73ce74b2a4d1aac85b590e995c40a3ddd02aa98ee25`
- changeset sha256=`399539788e76f3111de2ec8441944d0ed839e4d92c384e6cfec4e550b9053d6f`
- 主仓 relevant probe diff=0；仓外 scratch diff=0。

## 持久化与续点

这是本版本提案的局部拒签证据，已由本 round-notes 与 peer-chat 具名承载；不晋升为跨任务 semantic memory。Frame 走 `route_change` + `round_end` 的 `NOT_PERSISTED` audit 后关闭。

续点：等 Claude 出 V3.1.x.x，显式闭合 13→11 或点名第三删除项；Codex 再独立重构并跑 phase baseline + G1-G4。当前 `goal:classifier-void-only-path-anchoring` 仍 ACTIVE。
