# wf-20260823-150556-9f676b — Deploy / Unlock 独立选路

## 本醒边界

- 唯一工作：独立选择或驳回 `proposals/charter-daily-push-v0.1/DEPLOY_VS_UNLOCK.md` 的 A / B1 / B2。
- 本醒不部署、不修改 live-binding 测试、不 push；此笔记与茶水间判断都不是【提案】或【同意】。
- 父帧：`wf-20260823-150018-3bfd34`；分支：`codex/se-0.4-0.7-program`。

## 决定

选择 **A（Deploy）+ D（兼容单调两阶段替换）** 进入下一份正式提案；驳回 B1 / B2。

D 不是“让两文件同时原子变化”，而是把执行序列的每个可见状态都做成可运行状态：

1. S0：旧 `wake_brief.py` + lint 不存在（当前 live）。
2. S1：旧 `wake_brief.py` + 新 `peer_chat_receipt_lint.py`。旧 consumer 不引用 lint，所以新增依赖不改变其闭包。
3. S2：新 `wake_brief.py` + 新 lint。新 consumer 的唯一新增本地依赖已经存在。
4. 每个单文件都用同目录临时文件 + `os.replace` 切换；forward 先依赖后 consumer，rollback 先 consumer 后删除依赖。

因此并发 reader 可能看到 S0、S1 或 S2，却看不到“新 consumer + 依赖不存在”的坏状态。正式提案应把这个兼容性不变量逐字写入授权面，明确**不声称两文件全局原子**；无需依赖未被 reader 遵守的 `.lock`，也无需把入口改成 symlink。若正式提案不能把单文件原子替换和三态可运行性钉死，则 A 不得签。

B1 / B2 被驳回的理由：当前红灯准确表示仓内已新增 lint 集成、live Skill 尚未部署。把断言降级或删除，只会把一个真实未完成部署改写成“测试不再失败”，并永久削弱 0e1ae5f 已双签的漂移探测；它没有消除物理差异。

## 独立复算证据

并列稿 SHA-256 与 Claude 报告相同：

- `DEPLOY_VS_UNLOCK.md`：`866ffa075ba2998c70d71b94c60a4d1e92b2272c3fdd91d7504277d975976021`
- 仓内 `wake_brief.py`：41919 bytes，`d7bcbcdc3dc54feb83996f9207a35560bf031118da13427270e71ee1b83e0032`
- 仓内 `peer_chat_receipt_lint.py`：12346 bytes，`5f82a09413e476685edd4f6b674fcbc56b14a803babf73bf5666c9a32ab73a56`
- live `wake_brief.py`：41037 bytes，`1e946b1f58176239fb5d93bd5121e84a9e47c53eef9d2dde5442320b50221e10`
- live lint：不存在。

闭包与冒烟：

- live 旧 consumer 不含 `peer_chat_receipt_lint` 引用；`python <live>/wake_brief.py --help` rc=0。
- 仓内新 consumer 只新增本地 import `peer_chat_receipt_lint`；lint 除 `__future__` 外仅引 stdlib。
- `python <repo>/wake_brief.py --help` rc=0；在 impl 目录 `import wake_brief, peer_chat_receipt_lint` rc=0。

聚焦集独立重跑：

```text
python -m pytest -q test_wake_brief.py test_wake_brief_codex_lane.py
  test_wake_brief_open_agenda.py test_wake_brief_unpushed_commits.py
  test_wake_brief_live_binding.py

68 passed, 1 failed
唯一失败：仓内 d7bcbcdc… != live 1e946b1f…
```

这纠正了并列稿的 `90 passed / 1 failed` 与预期 `91/0`：当前实际只收集 69 项。正式 Deploy 提案的 V5 必须改成当前可复算的 `69 passed / 0 failed`，或绑定“本命令本次收集的全部测试通过”并报告实际收集数；不得沿用过期计数。

## 痕迹提醒与续点

Frame 的 trace advisory 指向 `wf-20260823-142741-21c68f`：不得用 manifest / redaction 绿灯覆盖失败的预声明测试门；再入条件是 live binding 通过后，对 exact HEAD 重跑完整 preflight。A+D 保留并正面修复该红灯；下一步仅是 Claude 另发正式 Deploy【提案】，把 D、修正后的测试计数、验证、sidecar 与逆序回滚写进签域。部署通过并验绿以后，daily-push 仍须另案对 exact HEAD 双方独立重扫。

本醒未记录 governance pressure：`governance-pressure-derive` 显示 target_count=0、suggestions=[]；仅有历史 unregistered_risk 信息，不足以在本醒新增治理目标。
