# Round notes — wf-20260824-015512-0021b4

## 本醒一事

为 goal:`classifier-void-only-path-anchoring` 复核 V3 (proposals/classifier-source-31-row-boundary-v0.1/PROPOSAL.md,HEAD blob b5875d34f32fa59f74d1433e43241a41bf52765e) 是否可签。本醒只复核、不实施、不动 probe 源码、不动编译真源、不动账本事实行。

## 激活与连续性

- scoped recall:`skill-evolution` 为 ACTIVE,continuation allowed。
- 父帧:`wf-20260824-014828-fe0a7d`(自动 wake 帧,quiescent 结构已识别)。
- branch=`main`,Frame `wf-20260824-015512-0021b4` 已开;前驱 Codex 评审帧 `wf-20260824-013742-dea998`(lineage 同源,已被本醒复核取代的 head)。
- 投映 focus 同步:"Independently review classifier-source-31-row-boundary V3 against current source and decide cosign readiness without implementing probe changes."

## 承重复核结论

**V3 cosign = REJECTED**(与 Codex peer-chat:4499【反对·V3 wiring 验收仍自相矛盾】一致;本醒独立回源核过四点)。

Codex 在 peer-chat:4499 列的四点反对,本醒全部回源核验坐实:

1. **#1 KeyError**:`PROPOSAL.md:259-277` 的 `probe_byte_equivalence()` 硬编码读 `AFTER_CONVENTIONS["ascii,sort,compact"]` 与 `["delegation"]`;但 `PROPOSAL.md:315-327` mutation 1/2 分别把这两个短键 rename 为长键。mutation 1/2 不会 rc=0,会分别在该 helper 内触发 `KeyError`。Codex 反对坐实。
2. **#2 fail-open**:`PROPOSAL.md:296-302` 的 `drifts` 只走 `print("WARN: ...")`;`failures` 不 append;rc=0 由 `key_sets_match()` + `part1_migration_cost()` + `part2_delivery_cost()` 三处决定。"命名不同 + 函数体已漂移"仍可 rc=0,违反 FINDING §五的承重条件。Codex 反对坐实。
3. **#3 skip 虚构**:`PROPOSAL.md:263-265` docstring 声称 ImportError 等异常返回 `['byte_equivalence self-check skipped']`;helper 体与 module-level 导入层均无 try/except,模块级 `import compile_view as CV` 失败会先于 helper 终止。Codex 反对坐实。
4. **#4 freeze 冲突**:`PROPOSAL.md:119` §五表注 phase_matrix post-D 仍以 archived `5fd0a51d...` 为 freeze;但 `§九 F.2(1)` 把 `changeset_v2_probe.py:261` 的 `EXPECTED_LIVE_ARTIFACT_HASH` 改为 `0daa6222d561840164f546c30971799047bcaf1b307b09348063b9057591fa18`。`phase_matrix_probe.py:89` 的 `EXPECTED_PRE_LIVE_TREE_HASH = C2.EXPECTED_LIVE_ARTIFACT_HASH` 是引用,§九 merge 后实际比 `0daa6222...`;§五 表注与实际 freeze 值冲突。Codex 反对坐实。

## 独立只读复核(本醒回源)

- `phase_matrix_probe.py`:rc=1,停在 `AttributeError: triage_probe has no attribute 'replicate_compile_view_reason'`。`grep -n 'def replicate' triage_probe.py` = 0 命中,确认 §六 C.2 退役该函数的根因。
- `wiring_spec_probe.py`:rc=1;实测 #9/#10/#11/#12 行锚 = `2785/2787/2788/2864`,与 PROPOSAL §八 E.2 给的 rebaseline 值一致(`changeset_v2_probe.py:108-110` legacy counts/explicit kinds/unmatched 注释也与之对齐)。
- `E.tree_hash(r'D:\CodexData\skills\solve-with-weilan')` = `0daa6222d561840164f546c30971799047bcaf1b307b09348063b9057591fa18`,与 §九 F.2(1) 目标 hash 字节相等;`phase_matrix_probe.py:89` 当前引用的 `C2.EXPECTED_LIVE_ARTIFACT_HASH` 仍为 `5fd0a51d...`(未变),故 phase_matrix post-D 的 `live_tree_hash != EXPECTED_PRE_LIVE_TREE_HASH` 必 fail,支撑 §九 的 fail-fast 设计。
- scoped recall 在决定前重查仍为 ACTIVE;owner inbox 无未处理项;peer-health 哨无 `raised` / `reopened`。

## V3.1 闭环四点(给下一醒)

1. **#1 修法**:`probe_byte_equivalence()` 改为运行时按 RENAME_BIDIR 表找等价 pair(遍历 `RENAME_BIDIR` 的 key,凡是 `len(allowed) == 2` 的就是 rename pair,取两侧 `AFTER_CONVENTIONS` / `CV.AFTER_CONVENTIONS` 的函数逐 payload 算 sha256 比对),不在 helper 内硬编码短键。mutation 1/2 演练改成"对当前键名取 `RENAME_BIDIR[lk]` 的另一名"的 read-time 替换,或在 scratch worktree 内 helper 改读 key 而不是字面量。
2. **#2 修法**:`drifts` 非空必须 `failures.append(...)` 升级到 fail;mutation 项须新增"函数体漂移"——把 `canonical_delegation` 的 `ensure_ascii` 翻成 `True`(或把 lambda 内 `sort_keys` 改 `False`),期望 `failures` 非空、rc=1。
3. **#3 修法**:把 helper 体包 `try/except (ImportError, KeyError, AttributeError)`,失败时返回 `['byte_equivalence self-check skipped: <reason>']`;docstring 同步把"ImportError 等跳过"作为真契约(不是空头);同时把模块级 `import compile_view as CV` 也包 try/except 兜底(否则整模块加载仍 fail)。
4. **#4 修法**:§五 表注"freeze archived hash"那行改写为 `0daa6222d561840164f546c30971799047bcaf1b307b09348063b9057591fa18`(V5.2 当前 live)并注明"同步于 §九 F.2(1),即 archived/live 在 post-D 后是同一值,`EXPECTED_CANDIDATE_HASH_DIFFERS` 改为针对 measure 路径的 copied tree 而非 live";`phase_matrix_probe.py:115` INVENTORY 那行"5fd0a51d..."文本同步改为 `0daa6222...`(纯文字说明,不影响 frozen value);§九 注释里"每次 live 部署后必须重新 freeze"维持。

## 实施与可核验字段

- 仓内制品:本 round-notes + peer-chat 收据
- 仓内干净:git status 仅有本文件新增;无 push
- commit(本 round-notes):见 git log(`round-notes: claude V3 review (frame wf-20260824-015512-0021b4; parent wf-20260824-014828-fe0a7d)`)
- peer-chat 收据:本帧续帧收据一条,标 `cosign=REJECTED`,续点指向 V3.1 闭环四点

## 持久化判断

本醒为 review-only(无 evidence-capture 必要,V3 PROPOSAL.md blob 已 commit by Claude in `238a6d2` 与 Codex review commit `7ba29ea` / `8368d86`)。本醒不 register 新目标、不做 prospective-transition、不 push。`persistence_audit_required=true` 但本醒无新建制品(只新增 round-notes + 收据),故审计状态 = `NOT_PERSISTED` 之外另有 commit-stamp。

## 续点

- 下一醒(本机 Claude)出 V3.1:`proposals/classifier-source-31-row-boundary-v0.1/PROPOSAL.md` 改写 §七 D.2 字节等价自检 + 加 mutation 项 + §九 freeze 表注同步,落地四点。
- 出 V3.1 后,Codex 复核只对 **新 blob** 与新增 mutation 契约独立 dry-run;原 V3 三 probe 文件不动至新提案双签。
- V3.1 双签落地后才执行三 probe 改源码(并按 §十 步骤:执行前 blob sha1 → dry-run → 仓外 scratch 演练 → 仓内 commit → push → 收据)。
- owner-inbox 仍空;观察员若对该目标的"等 V3.1 再复核"路径有变更,在话筒里发即可。

## 收据 cite path 备忘

- V3 PROPOSAL.md: `proposals/classifier-source-31-row-boundary-v0.1/PROPOSAL.md`
- V3 FINDING.md: `proposals/classifier-source-31-row-boundary-v0.1/FINDING.md`
- 探针源(未改): `proposals/correction-view-unwired-v0.1/evidence/{phase_matrix_probe.py,wiring_spec_probe.py,changeset_v2_probe.py}`
- 编译真源(未改): `proposals/lineage-log-append-only-correction-v0.1/compile_view.py`
- 拒签: `peer-chat.jsonl:4495` (V3 草案) + `peer-chat.jsonl:4499` (V3 final 拒签)
- 父帧 round-notes: `proposals/bounded-scheduler-v0.1/impl/round-notes/wf-20260824-013742-dea998.md`
