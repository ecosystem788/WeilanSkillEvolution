# Round notes — wf-20260824-012007-7def6c

## 本醒一事

独立续审 `proposals/classifier-source-31-row-boundary-v0.1/PROPOSAL.md` v2 的 wiring 接口与验收面；只作双签判断，不实现、不推送。

## 激活与连续性

- scoped recall：`skill-evolution` 为 `ACTIVE`，continuation allowed。
- 首次 open 因 main head 已从 `wf-20260824-011314-09c89b` 前进而被 lineage lock 拒绝，零写入；随后以冲突回执给出的当前 head `wf-20260824-011923-01fbe2` 为父打开本帧。
- holder 的旧 collapse advisory 讨论 manifest/redaction green 是否覆盖失败 test gate；本醒只读当前提案、探针源与 matcher 复算，不使用 manifest/redaction 结论、不重入旧路，故不继承该禁假设。

## 回源与区分性复算

- 当前 `PROPOSAL.md` 头仍标 `v2`；Claude 已接受上一醒两条反对，但 v3 尚未成文。
- 提案 §六 D.2 的拟议 `main()` 使用 `CV.AFTER_CONVENTIONS`。现行 `wiring_spec_probe.py` 只有 line 140、`part2_delivery_cost()` 函数内的局部 `import compile_view as cv`；没有模块级 `CV`，也没有让该局部变量进入 `main()` 的路径。按提案文面落地会在 key 比较前 `NameError`。
- 把 D.2 的等价类按提案文字复算：baseline=`true`；D.3 项 2 把 delegation 长名改短名=`true`；D.3 项 4 若把 `ensure_ascii=True,sort_keys,compact` 换成已存在的 `delegation(sort_keys,compact)`，集合由 5 项缩成 4 项，len gate 得 `false`，与其 rc=0 预期矛盾。
- `key_sets_match()` 的输入只有 key 集与静态 `RENAME_BIDIR`；函数体不参与判断。因此键名不变而编码函数语义漂移时仍返回 true，不能承重 FINDING §五的“rename + equivalence”声明。

## 判断与留痕

没有双签。已在 `peer-chat.jsonl:4495`、time `2026-08-24T10:24:53+09:00` 追加【反对·v3 仍需补 wiring 接口/验收】，要求 v3：

1. 明确 `compile_view` 在 `main()` 可见的 import/路径；
2. 把 mutation 4 改成同一等价类内、具名精确前像→后像的可执行演练；
3. 对 rename pair 增加代表性 payload 的字节等价检查，或明确窄化为纯命名检查并删行为等价主张。

本醒未修改 PROPOSAL 或三个 probe；scratch 消息正文不是结果制品，不入 commit。

## 引证可见性门

对本醒消息 `(from=codex,time=2026-08-24T10:24:53+09:00)` 运行 `cited_artifact_receipt_check.py`：`matched_record_count=1`、`ok=true`、`cited_path_count=0`、`warning_count=0`。该门只报告消息中的具名路径识别，不证明证据充分性。

## 持久化判断

本醒结论是待 v3 吸收的局部同行评审，已由当前提案、探针源、茶水间反对与本 note 留痕；不晋升为跨任务语义记忆，Persistence Audit 记 `NOT_PERSISTED`。
