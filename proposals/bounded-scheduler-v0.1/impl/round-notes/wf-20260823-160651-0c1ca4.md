# wf-20260823-160651-0c1ca4 — Deploy V5.1 独立裁断

## 帧与边界

- scope: `skill-evolution`
- branch: `main`
- relation: `continue`
- parent: `wf-20260823-160218-29dcc6`
- activation: scoped recall `ACTIVE`, `continuation_allowed=true`
- 本醒一事：独立审签 `peer-chat:4431` 的 Deploy V5.1；只有全部前置、V1–V7、RB1–RB5、witness 与 commit-stamp 字段可满足时才同意并执行。
- 明确不做：daily-push、classifier r4、其他 open agenda、push。

## 权威与方法

- 当前用户给出 `skill-evolution` 单一 scope 与有界回合纪律。
- `CHARTER.md` §三：部署须【提案】+ 独立【同意】；【反对】即不执行。
- `proposals/cosign-bytewise-binding-v0.1/CONVENTION.md` §一：本案是多文件实现/部署，不自动进入恰一文件逐字绑定窄惯例；V5.1 的五件字段是自愿加固，但一旦写入提案仍是签名对象。
- WeiLan Collapse Gate：核心假设被可靠证据否定时，坍缩最小无效单元并保留可复用结果。

## 独立复核结果

| 项 | 独立观测 | V5.1 声称 | 结论 |
| --- | --- | --- | --- |
| proposal artifact | 14849 B；SHA-256 `5d61c41353ab06e989a7420c6284634651883faa9337237f4e4e06f9513cbfa9`；130 LF；0 CRLF | 同值 | match |
| proposal / note commits | `0283e3e6a201c2a40860a9d8cc71d4a0ba42edb2`、`34073245d8eab32fa625095cf38afebcba5f2725` 均为 commit；当时 HEAD=`3407324…` | 同对象 | match |
| repo wake_brief | 41919 B；SHA-256 `d7bcbcdc3dc54feb83996f9207a35560bf031118da13427270e71ee1b83e0032`；1084 LF | 同值 | match |
| repo lint | 12346 B；SHA-256 `5f82a09413e476685edd4f6b674fcbc56b14a803babf73bf5666c9a32ab73a56` | 同值 | match |
| live base | wake_brief SHA-256 `1e946b1f58176239fb5d93bd5121e84a9e47c53eef9d2dde5442320b50221e10`；lint ABSENT | 同值 | match |
| line shape | 规范 LCS：base 1063 行、final 1084 行、LCS 1059，故 25 增 / 4 删 | 25 / 4 | match |
| import closure | wake_brief 新增本地 import 仅 `peer_chat_receipt_lint`；lint 仅 stdlib | 同值 | match |
| focused baseline | 68 passed / 1 failed；唯一失败 `test_cron_and_live_agent_wake_brief_are_byte_identical`，hash 对为 `d7bcbcdc… != 1e946b1f…` | 同值 | match |

工作树有大量既存未跟踪项且 `peer-chat.jsonl` 已修改；本醒未清理、stash、revert 或改写任何他人内容。

## 使 V5.1 失去签署资格的最小断裂

`peer-chat:4431` 明确声称“§1.5 sidecar 命名由 `<v5-frame-id>` 改 `<execution-frame>`”。但 commit `0283e3e` 中的 `V5_PROPOSAL.md` 实际为：

- 第 47 行（§1.3 forward 1）：`D:/CodexData/deployment-sidecars/<v5-frame-id>/...`
- 第 70–72 行（§1.5）：sidecar 目录和两份 witness 文件继续用 `<v5-frame-id>`
- 第 76 行：只把仓内 `SIDECAR_<execution-frame>.txt` stamp 改为 `<execution-frame>`，并称“不再使用 `<v5-frame-id>` 占位符”
- 第 119 行（§3.3）：仓外 sidecar 又改成 `<execution-frame>`

同一份 mandatory sidecar/witness 因而有两套具名占位符，茶水间对 committed artifact 的承重陈述也不成立。把两种占位符临场解释成同一个值虽可得到一个可运行实现，却会由执行者单方补造未被共同签署的 mandatory witness 命名，违反双签拒绝把现场判断伪装成既有授权的目的。

## 裁断与可复用 Trace

- provisional holder `approve_and_execute_v5_1` 坍缩；最小坍缩范围为当前 V5.1 deployment route，不否定 A+D、forward/rollback 分门或已核对的数值。
- 在 `peer-chat:4433` 发出【反对·V5.1】；未发【同意】，live Skill 未动，未 push。
- 引证可见性门：`ok=true`，matched_record_count=1，cited_path_count=0，warning_count=0；仅有三条历史 corrections 已知解析项。
- re-entry：commit-stamped V5.2 必须把 §1.3、§1.5、§3.3 的仓外 sidecar 目录、pre/post witness 文件名与仓内 SIDECAR stamp 全部统一绑定同一个 `<execution-frame>`，重新给出全文 hash 与 commit。

## 验证命令摘要

- scoped `memory-recall`: ACTIVE
- `lineage-show --scope skill-evolution`: valid=true；开帧时 main head=`wf-20260823-160218-29dcc6`
- `pytest -q test_wake_brief.py test_wake_brief_codex_lane.py test_wake_brief_open_agenda.py test_wake_brief_unpushed_commits.py test_wake_brief_live_binding.py`: 68 passed / 1 failed（唯一预期基线失败）
- 原始字节 SHA-256 / LF / CRLF 独立重算：如上表
- 规范 LCS 动态规划独立重算：25 added / 4 deleted

## 下一醒

只在新的 bounded `skill-evolution` scoped ACTIVE 回合中重审 commit-stamped V5.2；不得把本帧的拒签读成 V5.1 可执行授权。
