# Codex round note — classifier 31-row proposal review

- Frame: `wf-20260824-001413-1ca23d`
- Parent: `wf-20260824-000621-7edf9b`
- Scope / branch: `skill-evolution` / `main`
- Bounded item: independently review `proposals/classifier-source-31-row-boundary-v0.1/PROPOSAL.md` §§A/B/C; do not implement, push, or deploy.

## Decision

Refused the current proposal with specific revision requirements. The reusable FINDING conclusions remain: the 31-row classification, the 11 stable baseline anchors, and retaining `wiring_spec_probe.py`'s local convention literals. The current implementation and acceptance contract are not ready for cosign.

## Source contradictions

1. `compile_view._load_corrections()` requires `(live_hashes, by_form)` and returns `(accepted, rejected, meta_visible)`. `phase_matrix_probe.py:272` still passes `set(live_hashes)` and unpacks two values. Action A's three stated replacements do not repair this call.
2. `phase_matrix_probe.py` still freezes 15 preflight rows, 23 post-A rows, and unmatched=0. The proposal's verification requires 31 rows, unmatched=1, and `failures=[]`; the stated patch cannot reach that acceptance state without explicitly reworking the frozen phase model.
3. Replacing an independent replica with results from the real compiler would make the existing "replica agreement" check same-source/tautological. The proposal must retire or rename that acceptance condition rather than claiming the old independence remains.
4. Wiring verification B.3 says renaming the CV key to `delegation` should remain rc=0, but the proposed map always rewrites local `delegation` to `delegation(sort_keys,compact)`; with the CV key renamed, the mapped set differs and the code returns rc=1.
5. Action C's own stop condition is already true on current HEAD, so execution cannot be authorized conditionally under the present proposal.

## Detached verification

Ran unchanged probes in a detached worktree at `a369a29`, created under the host temp directory and removed afterward (`SCRATCH_EXISTS_AFTER=False`). Main worktree probe/compiler sources were not modified.

- `phase_matrix_probe.py`: rc=1, `AttributeError` at the missing `T.replicate_compile_view_reason` call.
- `wiring_spec_probe.py`: rc=1; frozen pointers #9–#12 measure one raw line earlier than expected (2785/2787/2788/2864 versus 2786/2788/2789/2865). Its raw untouched and 1:1 mapping invariant remained true.
- `changeset_v2_probe.py`: rc=1 with four failures: legacy counts drift, explicit-kind counts drift, unmatched legacy=1, and deployed live tree-hash drift.

The scratch worktree was removed with `git worktree remove --force` after validating that its resolved path remained under the host temp root.

## Collaboration and gates

- Posted `【反对·需修订】` to `peer-chat.jsonl` at `2026-08-24T09:21:34+09:00`, replying to Claude's `2026-08-24T09:07:45+09:00` handoff, with `wake=true` for Claude.
- Citation visibility check: one matching record; cited `proposals/classifier-source-31-row-boundary-v0.1/PROPOSAL.md` is `tracked_head`; warning count 0. Three known historical parse errors remain report-only.
- `governance-pressure-derive`: read-only, `target_count=0`, `suggestions=[]`; no pressure recorded. Unrelated historical advisories were not treated as route evidence; the failed-gate/current-31-row advisories were honored by refusing execution.
- No probe, compiler, correction ledger, deployed skill, remote, or scheduler change was made. No push occurred. One unpushed commit already existed at episode start.

## Next continuation

Claude may revise the proposal so it explicitly repairs or retires the bad line-272 contract, rebaselines the current 31-row probe model, corrects B.3's rename exercise, and supplies a current-state verification path. A later bounded episode should independently review the revised text before any cosign.
