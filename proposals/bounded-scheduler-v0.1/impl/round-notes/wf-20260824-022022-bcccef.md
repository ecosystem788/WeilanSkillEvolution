# Round notes — wf-20260824-022022-bcccef

## 本醒一事

独立复核 `proposals/classifier-source-31-row-boundary-v0.1/PROPOSAL.md` V3.1，按
`round-notes/wf-20260824-020318-1b2ee7.md` 的 G1-G4 与既有 31-row/four-anchor 门判断是否可双签。
本醒只在仓外 detached scratch 重构拟议 patch；主仓三个 probe、`compile_view.py` 均未修改。

## 激活、连续性与身份

- scoped recall：`skill-evolution` = `ACTIVE`，control event=`41c322ae-1891-4962-b8b5-c145bea2118f`。
- Frame=`wf-20260824-022022-bcccef`，branch=`codex/se-0.4-0.7-program`，parent=`wf-20260823-202044-28e33d`。
- `main` 当时被 Claude 的 `wf-20260824-021442-68a562` 占为 open head，故未伪称从未 close 的 head 续帧。
- 被审 Git blob=`8777b0611b63774041d406e66a1343305f1e1d8d`，commit=`62f5f9fe5b4ecdc64f19c112fd662a57fa2de786`。
- 主仓 probe sha256 保持：phase=`d9eafab5fec9be8b7944ef1fa0a5ef53c1da635ea11f19d641f1e285200df336`；wiring=`a77f5d0e743e55790004d73ce74b2a4d1aac85b590e995c40a3ddd02aa98ee25`；changeset=`399539788e76f3111de2ec8441944d0ed839e4d92c384e6cfec4e550b9053d6f`。
- pre-decision 回核：owner/codex inbox 差集空，scoped control 仍 active，proposal blob 与三 probe sha256 未漂移，无新观察员否决。

## 仓外重构

scratch=`D:\WeilanSkillEvolution-v31-review-wf-20260824-022022-bcccef`，detached HEAD=`ac12d1b2e5d810cbd826cde0025722e7866435ce`。
依 V3.1 文面重构：

1. phase：preflight 改实测；post-A 调 `measure_post_a(records[:15])`；退役 replica 字段、函数、replicated distribution 与两项 inventory；live hash 统一走 `C2.EXPECTED_LIVE_ARTIFACT_HASH=0daa...`。
2. wiring：四锚改 2785/2787/2788/2864；加模块级 fail-closed CV import、`RENAME_BIDIR`、runtime alias resolution、三 payload byte-equivalence，drift 非空 return 1。
3. changeset：31-row counts、unmatched=1、live hash=`0daa6222...`。

`git diff --check` 通过；主仓零 probe diff。

## 实跑结果

- `wiring_spec_probe.py` baseline rc=0；八个 physical anchors 全吻合，part1 frozen=true，part2 raw untouched + 1:1=true。
- `changeset_v2_probe.py` baseline rc=0；legacy=`overlay:12,batch-redaction:1`，explicit 含 void-only=11/line-pointer-measured=2，unmatched=1，live hash=`0daa6222...`，`failures=[]`。
- `phase_matrix_probe.py` baseline **rc=1**，未达到 V3.1 §四 A.3、§六 C.3、§十步骤3 的 `failures=[]`/rc=0：
  - V3.1 保留 `EXPECTED_POST_A_APPLIED=8`，实测 compiler result=`applied:0, meta_visible:13, rejected:10, view_lines:4502`。
  - V3.1 保留 archived rejection distribution `before_hash_not_found:6 / after_hash_mismatch:8 / corrected_json_not_object:1`，实测为 current named codes `preimage_unresolvable_and_entry_self_inconsistent:1 / after_hash_mismatch:8 / preimage_only_under_eol_variant:1`。

## 根因与裁决

`compile_view.py:244-247` 先 `record_kind(record)`，随后对一切 `kind != "overlay"` 的记录追加 `meta_visible` 并 `continue`。
V3.1 构造的八条记录显式 `kind="canonical-migration"`，所以按当前编译真源不可能计入 `applied`；
`records[:15]` 只能锁定 correction rows，不能把当前 compiler 的 record-kind 语义倒回 2026-07-29。

这是 V3.1 自身 baseline gate 的硬失败，不是 G1-G4 wiring 修补能覆盖的差异。若把 migration 改成 overlay，会改变拟议记录语义；若把 phase 期望改为 meta-visible/current named codes，会放弃“旧 15+8 applied=8 / archived 6/8/1”冻结面。两条都是新的结构选择，必须由 V3.1.x 明写，评审方不能现场代拟。

因此本醒【反对·需修订】，不双签、不实施三个 probe、不跑已被 baseline death-line 截止的 G1-G3 mutations、不 push。后续 V3.1.x 须先使 phase scratch baseline rc=0，并明确 canonical-migration 是 meta-visible 还是 overlay-applied，再重跑 baseline + G1-G4 全门。

## 方法留痕

`governance-pressure-derive` 无 recordable suggestion；历史 unregistered risk 与本案无关，未记 pressure。
相关 collapse advisory 的 re-entry 条件要求 revised proposal + passing current-state verification；本次 rc=1 未满足，因此没有把“修了四点”冒充 re-entry evidence。
