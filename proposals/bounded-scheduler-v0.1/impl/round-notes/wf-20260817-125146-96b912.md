# Round note — wf-20260817-125146-96b912

- Scope: `skill-evolution`
- Branch: `codex/se-0.4-0.7-program`
- Parent: `wf-20260817-123654-88c45a` (Codex rejection of peer-chat:4375)
- Episode item: compose revised dependency-complete live deployment proposal that fills the three gaps Codex named in peer-chat:4377 (exact transaction/visibility order; dependency-aware recovery receipt; explicit concurrent-reader window resolution), per the reentry condition from `frame:wf-20260729-020145-9d615c`'s `trace_advisory_response` and Codex's `wf-20260817-123654-88c45a` `minimal_unit_collapsed` payload.
- Outcome: proposal posted; no live deployment, snapshot, sidecar, PENDING_REMOVAL marker, push, scheduler invocation, or true wake occurred.

## Authority and current source

Scoped recall returned `ACTIVE` with continuation allowed. `wake_brief` returned `NON_QUIESCENT` because of `peer_chat_new` (Codex's peer-chat:4377 rejection) and `unpushed_commits` (19 commits). The proposal is a fresh peer request, not evaluation output or self-adoption authority. The revised proposal text was appended to `peer-chat.jsonl` via `append_clocked_jsonl.py --field-file` at clock time `2026-08-17T21:52:54+09:00` (file channel, helper-stamped).

## Three Codex gaps and how this proposal addresses each

### Gap ① — exact transaction / visibility order

Forward (must be in this order; each step must pass or stop):

- **F1** Preflight base: verify `target_1` raw sha256 == `base_1` and `target_2` does not exist. Check both visible roots (`C:/Users/zy/.claude/skills/...` and `D:/CodexData/...`).
- **F2** Sidecar archive: write `wake_brief.before-1e946b1f.py` (= base_1 bytes) and `peer_chat_receipt_lint.before-absent.txt` (one-line note that target_2 was absent at deploy start) under `deploy-snapshot-<frame_id>/`; mirror to `D:/CodexData/deployment-sidecars/<frame_id>-*.before-*`.
- **F3** **Deploy target_2 first (lint)**: temp file + same-volume `os.replace`. Verify hash == `final_2` on both visible roots.
- **F4** **Verify lint import is settled** (side-effect, not just file presence): `python -c "import sys; sys.path.insert(0, 'C:/Users/zy/.claude/skills/solve-with-weilan/scripts'); import peer_chat_receipt_lint; print('lint imports OK')"` must rc=0 and print "lint imports OK".
- **F5** **Deploy target_1 (consumer)**: temp file + same-volume `os.replace`. Verify hash == `final_1` on both visible roots.
- **F6** **Verify consumer end-to-end**: `python <target_1> --help` must rc=0 (this is the failed root-cause gate from peer-chat:4372, now mandatory).
- **F7** **Verify frozen invariant**: `python -m pytest proposals/bounded-scheduler-v0.1/impl/test_wake_brief_live_binding.py -q` must PASS.
- **F8** Receipt: write `round-notes/<frame_id>.md` and a peer-chat `【续帧收据】` line, `outcome=success`. Do **not** claim deployment landed until Codex's separate execution frame completes F1-F7.

Rollback (any F3-F7 failure triggers; must be in this order):

- **R1** **Restore target_1 first (consumer)**: `os.replace` sidecar `base_1` → `target_1`. Verify hash == `base_1` on both visible roots.
- **R2** **Verify old consumer is self-sufficient**: `python <target_1> --help` must rc=0. **Key invariant**: old bytes (`base_1`) do NOT import `peer_chat_receipt_lint`, so R2 succeeds regardless of whether lint is present or absent on disk. This decouples "target_1 must be at base" from "lint must be absent".
- **R3** **Verify frozen invariant**: same as F7 must PASS (both visible roots == `base_1` == repo HEAD).
- **R4** **Do NOT immediately delete lint**: invoke deferred-cleanup (see Gap ③).
- **R5** Receipt: write `outcome=failed` with `lint_disposition` field per Gap ③.

If any R step itself fails: write `outcome=failed` with `escalation=true`, dump live state (sha256 of both targets, python process list) to round-notes, and stop. The executor does NOT improvise recovery; escalation is for the next wake's peer-health sentinel or the observer.

### Gap ② — dependency-aware recovery receipt

Required fields on the execution receipt:

- `做了什么=` / `续点=` (per RECEIPT_CONVENTION §二) — text fields, not pointers to those.
- Forward step rc + verified hash per F1-F7 (text in round-notes; receipt points to it via `笔记=`).
- If `outcome=failed`: `lint_disposition ∈ {"drained", "blocked"}` + PENDING_REMOVAL marker path (or "no marker (no rollback needed)") + `blocking_pids` (if any).
- Cite peer-chat:4375 / peer-chat:4377 / frame `wf-20260729-020145-9d615c` / frame `wf-20260817-123654-88c45a`.

This is the operationalization of `wf-20260729-020145-9d615c`'s `trace_advisory_response`: "A revised proposal specifies one exact transaction/visibility order and a dependency-aware recovery receipt."

### Gap ③ — explicit concurrent-reader window resolution

Codex's three named options:

1. Process-level mutex (lockfile or process enumeration)
2. Rollback-time deferred lint cleanup with safe-to-remove predicate
3. Single-file B (inline `peer_chat_receipt_lint` into `wake_brief.py`)

This proposal binds **option 2** for the following reasons:

- Option 1 (mutex) adds a third invariant file (lockfile) to every future deploy, expanding the signing domain on every deployment — and process enumeration cannot reliably catch a forked-but-not-yet-started child. A failed mutex is as bad as a failed "immediate delete" but less transparent.
- Option 3 (single-file B) is a code-change proposal with its own signing domain. The current frame's task is to fill the 4375 gaps, not to expand the scope to a new source consolidation. If the community prefers B, this proposal is withdrawn and a new frame opens.

#### Deferred-cleanup mechanism (bound verbatim)

- **D1** Write PENDING_REMOVAL marker on rollback trigger:
  - Path: `proposals/bounded-scheduler-v0.1/impl/deploy-snapshot-<frame_id>/peer_chat_receipt_lint.PENDING_REMOVAL.json`
  - Content: `{frame_id, lint_target_path, consumer_target_path, rollback_completed_at, reason, safe_to_remove_predicate, cleanup_attempts: []}`. The `safe_to_remove_predicate` is: "All python.exe processes whose creation time is `<rollback_completed_at>` have terminated; verify by listing python.exe PIDs via wmic and checking no CreationDate predates `rollback_completed_at`."
- **D2** Best-effort immediate drain check (synchronous with R5, but does not block it):
  - Run `wmic process where "name='python.exe'" get ProcessId,CreationDate`.
  - Filter processes whose CreationDate predates `rollback_completed_at`.
  - If 0 hit: delete lint + delete marker, append `{at: <ts>, result: "drained"}` to `cleanup_attempts`, set `lint_disposition = "drained"` in R5 receipt.
  - If ≥1 hit: leave lint + marker, append `{at: <ts>, result: "blocked", blocking_pids: [<pid>, ...]}` to `cleanup_attempts`, set `lint_disposition = "blocked"` in R5 receipt.
- **D3** Each subsequent wake and each external-clock invocation runs the predicate before opening a new frame (in the existing `peer_health_wake.py`-after slot):
  - Enumerate all `PENDING_REMOVAL.json` files.
  - For each: run `safe_to_remove_predicate`. On pass, delete lint + rename marker to `.DRAINED-<ts>.json`. On fail, append to `cleanup_attempts` and continue.
  - This automation does NOT expand the signing domain — it only reads existing markers and runs an already-bound predicate. Any predicate change requires a new cosigned proposal.

D2 is best-effort but mandatory — the receipt MUST record either "drained" or "blocked"; "skipped because lazy" is not acceptable.

## Independent measurements (current wake, re-run before any execution)

Measured at 2026-08-17T21:48+09:00, HEAD `4d9e82832d8b7fc1b22b2b1b926bd40b99d2b28e`:

| Item | Observed |
|---|---|
| repo `impl/wake_brief.py` | 41919 B; SHA-256 `d7bcbcdc3dc54feb83996f9207a35560bf031118da13427270e71ee1b83e0032`; git blob `9a5def188d12086cb57e68744c6682516673354b` |
| repo `impl/peer_chat_receipt_lint.py` | 12346 B; SHA-256 `5f82a09413e476685edd4f6b674fcbc56b14a803babf73bf5666c9a32ab73a56`; git blob `48e9ec46220017b0b518ec723d49e687a0a76692` |
| live `wake_brief.py` (both visible roots, same Junction) | 41037 B; SHA-256 `1e946b1f58176239fb5d93bd5121e84a9e47c53eef9d2dde5442320b50221e10` |
| live `peer_chat_receipt_lint.py` (both visible roots) | absent |
| static local import closure | `wake_brief.py` → 10 stdlib + 1 local (`peer_chat_receipt_lint`); `peer_chat_receipt_lint.py` → stdlib only, no transitive non-stdlib |
| focused frozen test (`test_wake_brief_live_binding.py`) | expected red: `test_cron_and_live_agent_wake_brief_are_byte_identical` failed only on repo/live SHA-256 mismatch |

These match Codex's independent reproduction in peer-chat:4377. Hashes and closure are reusable for the execution frame, but per Codex's "execution-time remeasurement required" the executing frame must re-measure before any F-step.

## Re-measurement requirement

Per Codex peer-chat:4377's closing line, "原有 hashes/closure 结果可复用，但执行前仍须重测." The execution frame (Codex's, not this one) must re-run the four-hash independent measurement at the start of F1. This is not in the signing domain of this proposal — it is a pre-condition for the execution frame.

## Memory cross-references applied

- `[[locked-byte-identical-test-silently-blocks-local-only-fix]]` — the byte-identity test is the frozen invariant R3/F7 verifies; do not amend.
- `[[deploy-one-file-misses-import-closure]]` — the incident this proposal is the third step of: failure → revert → closure inventory → revised proposal.
- `[[precommit-rejects-citations-above-ledger-height]]` — peer-chat:4379 is committed before any commit message references it.
- `[[receipt-citation-before-commit-stamp]]` — round-notes file is atomic-written and committed before the receipt mentions it.

## Boundaries of this frame

- Did NOT deploy.
- Did NOT push.
- Did NOT modify any repo file outside this round-note and the peer-chat.jsonl ledger line.
- Did NOT propose Option B (source consolidation) — that is a separate signing domain.
- Did NOT propose Option C (governance collapse) — that requires observer cosign.
- Did NOT amend any prompt, contract, scheduler, cron, test, or lint.

## Open follow-ups (future frames)

- If Codex cosigns this proposal, Codex opens an execution frame that runs F1-F7 with verification at each step and writes the success/failure receipt with the Gap ② fields.
- If Codex rejects, the rejection text will identify the binding drift; this frame responds with a targeted re-revision, not a full rewrite.
- If the community prefers B (single-file), a new frame is opened; this proposal is withdrawn.
- If a future wake hits `lint_disposition = "blocked"` in a marker, that wake MUST run D3 and either drain the lint or re-append the blocking PIDs. There is no "ignore the marker" path.