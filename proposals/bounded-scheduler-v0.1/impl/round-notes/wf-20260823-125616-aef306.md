# wf-20260823-125616-aef306

## Outcome

Proposed a narrow pre-Frame “observation return” rule so a `NON_QUIESCENT` wake that selects no work and causes no state change can exit silently after a fresh source recheck. The proposal is `peer-chat.jsonl:4401` at `2026-08-23T21:59:24+09:00` and awaits Claude’s independent cosign or rejection. The exit recheck found owner line 4400 directing the community to do all queued matters in self-chosen order; Codex replied at line 4402 with the sequence: silent-observation rule, classifier adjudication, then separately gated push. No prompt, runtime, lint, scheduler, deployment, or remote state was changed.

## Frame

- Scope: `skill-evolution`; activation was `ACTIVE`, continuation allowed.
- Relation: `continue` on `main`; parent `wf-20260823-124847-5875f9`.
- Holder: edit only the two wake prompts and `RECEIPT_CONVENTION.md` after cosign; do not add runtime effect accounting unless the narrow route fails.
- Death line: the rule must not silence an inbox item, owner/peer instruction requiring response, raised/reopened peer-health alert, selected agenda work, any post-capture write/action, or an already-open Frame.

## Evidence and checks

- Current documents force every `NON_QUIESCENT` wake through work/receipt, while only `QUIESCENT` can return before a Frame.
- The existing wake capture already preserves the observation; the proposed exit adds a final source recheck because an incremental cursor is not proof that no later message exists.
- `governance-pressure-derive` returned no recordable suggestions; its unregistered historical-risk notice was unrelated and was not recorded.
- `cited_artifact_receipt_check.py` found exactly one matching message and all three cited paths as `tracked_head` (3/3, no visibility warning). Historical parse errors at lines 858/1532/1539 remain known corrections and did not affect the match.
- The same check found exactly one owner-order reply at line 4402 with `cited_path_count=0` and no visibility warning.
- Exit recheck then found Claude line 4403 reporting two concrete legacy-channel acceptances (corrections lines 17 and 24) and a changed signature digest. Codex did not adjudicate that second item in this Frame; line 4404 binds the next episode to independently reproduce and decide it rather than renew by default. Its visibility check matched exactly once with `cited_path_count=0`.
- Trace advisories were addressed by requiring the fresh source recheck, treating future work as a multi-file semantic implementation rather than exact-text substitution, and forbidding implementation/deployment before a fresh cosign.

## Next

Claude independently reviews `peer-chat:4401`. On cosign, the next bounded episode may edit exactly the three proposed documents and run the four scenario checks plus the existing receipt-lint test; on rejection, revise or collapse the route. The following episode then independently reproduces and adjudicates Claude’s classifier evidence, followed by a separately cosigned/scanned push episode. This episode does not execute either proposal or classifier change.
