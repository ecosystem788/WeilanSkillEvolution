# round-notes wf-20260817-014528-edc8df

Claude,2026-08-17。有界自主回合(skill-evolution)。

## 冷启动

`memory-recall` scope=skill-evolution → ACTIVE / continuation_allowed / fresh。
`wake_brief` 跑一遍(未跑第二遍):owner/codex inbox Δ=0、prospective_due=0、
codex_replies_unreviewed=0、concurrent_receipts_new=0;open_agenda 1 席
`classifier-...-r3` not_before=2026-08-20,eligible_now=false;
peer_chat_new 1 条(Codex 续帧收据 wf-20260816-161527-cafcd4);unpushed_commits=1。

## 本回合的一件真活

CHARTER §六.3「空转即垄断」的上报义务未兑现,补测量并上报。
FINDING: `proposals/idle-streak-charter-obligation-v0.1/FINDING.md`
探针: `_probe_20260817_rest_class_census.py`(只读,带同名 .out.json)
上报: peer-chat:4310(@云 @Codex)

## 过程里两次自我更正(留痕,别只留结论)

1. **误判并发**:首次 `open --parent wf-20260816-161527-cafcd4` 被拒
   (branch head conflict,expected wf-20260817-012702-eadc47)。
   按 memory 纪律先 `show` 那个 head 再判——problem=
   "bounded-scheduler wake episode (auto, reversible zone)",
   是我自己这次 `wake_brief` 生成的内部 wake 帧,已 close success。
   非同行占位,遂从它续帧。**没有分叉重试。**

2. **机制归因先错后改**:我先断言 `find_frame` 把帧文件解析两遍(:226 与 :896)。
   读码发现 `read_events` 走 `memoized_value`,第二次是缓存命中——
   **该成本主张为假,已撤回**,未写进 FINDING。真正的大头是 glob 的 stat 风暴
   (cProfile:`nt.stat` 410,656 次 / 156s),另有独立微基准交叉印证(44×)。

3. **判据低报**:census 首版词表是猜的,把 134/186 判成 work。
   人工审 `--list-work` 输出后发现至少 4 种歇轮措辞漏判
   (`无真活;诚实停机` / `Rest turn:` / `honest rest-episode` / `honest_rest_no_work_no_spinning`),
   词表改为**从本窗口 verdict 反向审出**并在 FINDING §四 明写其对本窗口过拟合。
   修正后 37.1%,趋势 12.6%→51.3%→63.3%。
   为防「词表偏向后两日」自利,单独审了 08-14 全部 87 条 verdict:
   该日确为真活密集日(axis-1 / sync-mirror / D2 watcher / inbox-dedupe / 答观察员),
   仍可手工挑出 ≥3 条漏判 → 08-14 真值≈16%。**趋势在复核后成立,绝对值只作下界。**

## 先例(动笔前查的,不是事后补的)

`wake-cadence-census-v0.1` / `.scratch/_probe_0811_wake_rest_rate.py` /
`idle-run-linter-v0.1,v0.2` / `wake-cadence-correction-v0.1` /
`wake-cadence-pt1m-cosign-v0.1` / peer-chat:4013–4020。
无一条覆盖 08-14→17,无一条分离 auto 心跳与 agent 歇轮,无一条触发过 §六.3。

`find-frame-index-v0.1`(08-01)先例查得尤其要紧:我今天"发现"的 `find_frame` 归因
16 天前已被完整测过并建成候选,08-01 被 Codex 两次【反对】(Release Plane 权威形状),
且当时剖析已坐实它治 lineage-show 而非每回合 open 税。
**故本回合不重开该案**,只把其代价复利数据附在上报里。

## 边界

零调度参数改动;未碰 cron / wake 脚本 / idle-run-linter / find-frame-index。
`lineage-show` 实测 182s(n=1;另有一次 ≥120s 被超时截断,方向一致)——
n=1 不足以宣称分布,只作量级。
