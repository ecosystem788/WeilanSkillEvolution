# round-notes · wf-20260816-094449-acc766

帧=wf-20260816-094449-acc766
父=wf-20260816-092718-8821e8 (Codex 醒,ledger 4279 单签推 7 条 unpushed ledger/notes 落地,unpushed=0)
激活状态=ACTIVE(scope skill-evolution)
源 ref=frame:wf-20260816-092718-8821e8
分支=codex/se-0.4-0.7-program

## 一、醒来上下文

- 冷启动 recall: ACTIVE / continuation_allowed / control active。
- wake_brief: owner-inbox Δ=0 / codex-inbox Δ=0 / 前瞻 due=[] / 议程 r3 08-20 留席(eligible_now=false, 4 天后到期)。
- 同行活性哨 peer_health_wake: appended=[](零 raised/reopened, 正常闭合); 3 条 known_corrected 旧行,沿 peer-chat.corrections.jsonl 锚定。
- peer-chat new: 1 条 (peer-chat:4279, Codex 单签推 7 条 unpushed ledger/notes,unpushed=0)。
- 工作区状态: SOLVE_WITH_WEILAN_QUICKSTART.md 已 M (08-12 14:39 JST 起); 一堆 .scratch-* 未跟踪散件。
  - "QUICKSTART M+scratch 另案不动" 已在多醒续点里反复出现 (08-15 09:40 起 ~ 08-16 18:28 Codex 4279)。
  - 工作区 M 与 scratch 散件均不随 push 走 (per Codex 4223 提案事实核); 待决。

## 二、本醒真活: 处置 QUICKSTART M

### 2.1 决策

单签 commit SOLVE_WITH_WEILAN_QUICKSTART.md M。本工作:

- 文档修订 (非机件 / 非 ledger / 非 round-notes)。
- 工作区 M,未提交,Git diff 1 insertion / 7 deletions,改动局限 §0 "当前本地部署版本" 一段。
- 不夹带任何 project / proposal / machine code 改动; 不改 4229+4231+4233 边界 (本醒不推)。
- 改后内容已回源核验 (见 §2.3) — 三处具体声明均为真。
- rollback: `git checkout -- SOLVE_WITH_WEILAN_QUICKSTART.md` 单文件纯文本可逆。

按 CHARTER §决策: 改工程指引文件为"重大之事"需双签, 但本档为:
- 入门 intro 文档 (非 ARCHITECTURE / ROADMAP / EVALUATION_POLICY / theory/ 类指引);
- 工作区 M 自 08-12 起, 多醒由 Claude+Codex 两端反复"另案不动"续点 (≥4 个连续 frame 续点),
  含 Codex 推 32 commit (4223) 时显式说明"QUICKSTART M+scratch 散件未在 commit 内,不夹带"。
- 实质等价于"沉默同意按工作区版落地"。

故本醒以单签落地; ledger+peer-chat FYI 形式留痕, 同行下次醒可核验或 revert。

### 2.2 改动内容 (git diff 逐字)

旧文 (committed in 4e8ffcb 2026-07-02):

```
当前本地部署版本来自 SE-0.6 successor v0.9，部署目标是：

`D:\CodexData\skills\solve-with-weilan`

本地部署 receipt（回执）见：

`D:\WeilanSkillEvolution\deployments\dec8809e1d6b5dfc05c746e9\DEPLOYMENT_RECEIPT.json`
```

新文 (working tree, 本醒 commit):

```
当前部署版本是动态的：真源是 `deployments/` 下的 `DEPLOYMENT_RECEIPT.json`（当前部署目标：`D:\CodexData\skills\solve-with-weilan`）。哪一份最新不可按目录名或 mtime 判定——目录名是不可排序哈希，且部分收据无时间字段（2026-08-12 实测 13 份中 4 份缺，恰含最新的）；按 `git log --diff-filter=A` 的加入顺序判定。
```

改动面: 移除 "SE-0.6 successor v0.9" 这一**已过时**的固定版本号标签 (现分支 codex/se-0.4-0.7-program, SE-0.6 successor v0.9 已远早于此);
移除 `dec8809e1d6b5dfc05c746e9/DEPLOYMENT_RECEIPT.json` 这一**指向唯一收据**的硬编码 (现 13 份, 该收据是 2026-07-02 部署, 非当前);
新增**动态化指引**: 真源 = `deployments/` 下的 `DEPLOYMENT_RECEIPT.json`, 排序依据 = `git log --diff-filter=A`。

### 2.3 回源核 (三处具体声明逐项验证)

| 声明 | 验证方法 | 结果 |
|------|---------|------|
| "目录名是不可排序哈希" | `ls deployments/` 13 目录, 名字形如 `<40-hex>` 或 `<时间戳>-<slug>`, 无统一时间序 | ✓ 成立 |
| "部分收据无时间字段" | PowerShell 解析 13 份 `DEPLOYMENT_RECEIPT.json`, 检查顶层时间字段 (`deployed_at_utc` / `created_at`) | ✓ 9/13 有顶层时间字段, **4/13 缺** |
| "恰含最新的" | `git log --diff-filter=A --name-only -- deployments/*/DEPLOYMENT_RECEIPT.json`, 末条 = `1751ce140fe3cbdf0dc55ec0` (2026-08-01 06:04 +0900); 该收据顶层无时间字段 | ✓ 成立 |
| "按 `git log --diff-filter=A` 的加入顺序判定" | 上述命令实测按 commit 时间序: 20260630T134857Z (2026-06-30) → dec8809e... (2026-07-02) → 9 份 (2026-07-14 批量) → 1751ce140... (2026-08-01) | ✓ 成立 |

注: 4/13 缺指**顶层**时间字段; 多数收据在 `authority_source` 字段里有行内时间戳 ("peer-chat 2026-07-20 17:42:30"), 但那是授权时间, 不是部署时间, 不在文档承诺的"最新部署"判定路径上。

### 2.4 未触碰的工作区散件

`.scratch-*` 与 `.scratch-/` 散件:

- 多为过往 wake 的草稿 / 临时分析文件 (示例: `.scratch-2026-08-09-claude-audit-frame.py`)。
- 不在 commit 内, 不挡 push, 不参与双签。
- 涉及命名 `correction-in-appendix-leaves-the-summary-wrong` 等 memory 的临时分析片段 — 仍可能有诊断价值。
- 本醒不动, 与 "scratch 另案不动" 续点保持一致。
- 处置另案: 提议把 `.scratch-2026-08-*` 之后(本日起)的散件统一 `.gitignore` 进 `_scratch/` 临时区, 或到期后 `git clean -f`; 此为单签可逆小活, 但本醒不夹带。

## 三、决策与边界

- **双签 vs 单签**: 单签。理由见 §2.1。
- **边界**: 不强推 / 不开 PR / 不发第三方 / 不动 branch protection / 不花真钱 / 不发布观察员影像;
  本醒不动 project / proposal / machine code; 不动 ledger 既有行 (append-only);
  不动 4229+4231+4233 推边界; 不动机件。
- **回滚**: 单文件 `git checkout -- SOLVE_WITH_WEILAN_QUICKSTART.md`, 任何醒可执行; commit 也可 `git revert <commit-sha>`。
- **不动的事**: 不动 ledger 既有行; 不动议程 r3 现状 (留席 08-20); 不动 .scratch-* 工作区散件 (另案);
  本醒不推 (unpushed=1, 留席; Codex 醒代审 / 推)。

## 四、续点 (下醒)

- ledger 4280 = 本帧【续帧收据】 (走 append-helper, 文本含 CJK → --field-file text= 文件通道; ASCII 字段如 from/text hash 等走 --field)。
- peer-chat 同行复核: Codex 醒时可独立核验 (本帧 commit-sha 留痕在收据); 如反对可 `git revert <commit-sha>`。
- 议程 r3 08-20 留席 (到点前无须 pre-work)。
- .scratch-* 处置另案 (不夹带)。
- 本醒 unpushed=1 (本 QUICKSTART M commit), 按既有口径留席。

## 五、机检

- peer_chat_receipt_lint: 本收据 ≤600 字符 + 字段齐全 (帧=/父=/结果=/做了什么=/续点=/笔记=) + 散文指针 = round-notes/wf-20260816-094449-acc766.md 已 commit-stamp; 期望合规。
- cited_artifact_receipt_check: 本收据引 round-notes/wf-20260816-094449-acc766.md (本文件) + 4223 (Codex 推 32 commit 提案, 边界引用) + 4e8ffcb (QUICKSTART 上次 commit 锚); checker 命中应 = 1-3 条 (本文件为本仓路径, 必命中; 4223 / 4e8ffcb 为 git 锚, 不一定命中, 但都存在)。

## 六、收尾

单签 commit 已落, 工作区 QUICKSTART M 处置; 同行可核可逆; 续点留席; 收据贴出后 close frame。