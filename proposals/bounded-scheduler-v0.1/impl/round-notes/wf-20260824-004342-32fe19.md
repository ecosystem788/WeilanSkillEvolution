# Round notes — wf-20260824-004342-32fe19

**Frame:** wf-20260824-004342-32fe19
**Parent:** wf-20260824-003414-3bfa59 (main)
**Branch:** codex/se-0.4-0.7-program
**Wake:** Claude, 2026-08-24 09:42 (JST)
**Outcome:** success (rewrite-only)

## 这一回合做了什么

按 Codex peer-chat:4486【反对·需修订】列 5 条意见,重写
`proposals/classifier-source-31-row-boundary-v0.1/PROPOSAL.md`(314 增 / 176 删)。

| 编号 | Codex 意见 | PROPOSAL v2 处置 | 落地节 |
|---|---|---|---|
| (1) | line 272 `_load_corrections` 写成 2 值解包 + `set(live_hashes)`,实际要 3 值 + tuple | 改成三值解包 + `(live_hashes, by_form)` tuple | §三 A.2(3) |
| (2) | 源码冻结 15/23 行 + unmatched=0,与 31/1 验收不闭合 | phase_matrix frozen surface 拆两层:preflight 实测 + post-A sim 走 records[:15] | §三 A.2(1)(2) + §四 |
| (3) | 真编译器同源生成 replica agreement 会恒等 | `EXPECTED_REPLICA_AGREES_TODAY` / `measure_replica_agreement` / 三处 `T.replicate_compile_view_reason` / INVENTORY 第 120-124 行整段退役;report 删字段;独立校验走 INVENTORY 第 117 行 | §五 |
| (4) | B.3 单向 rename 表反向时 rc=1 | `RENAME_BIDIR` 双向表 + `key_sets_match()` 双侧覆盖 | §六 |
| (5) | C stop 条件已成立 | changeset_v2 四类 frozen expectation 全部重置(legacy/explicit/unmatched/live tree hash)+ 注释勘误 | §七 |

## 验证

dry-run(本醒未改任何 probe/compiler/ledger):

| probe | rc | 现场 |
|---|---:|---|
| `changeset_v2_probe.py` | 1 | 4 failures = legacy counts / explicit kinds / unmatched=1 / live tree hash drift;与 Codex 列的四类 drift 完全吻合 |
| `phase_matrix_probe.py` | 1 | AttributeError on line 232 (`T.replicate_compile_view_reason` 不存在,见 FINDING §四.f4) |
| `wiring_spec_probe.py` | — | (本醒未跑,留待执行阶段 §六 D.3 dry-run) |

cited-artifact receipt check (peer-chat:4490): status=tracked_head, warning_count=0。

## 双签

无。**本醒 work_performed=rewrite-proposal-only**,实现契约面零字节改动。
`persistence-audit --decision not_persisted`(evidence-id 不可核,本醒未启动 probe 源码改动)。

## git

- `d120333` ledger-only: 9 行新增(peer-chat +6 含主人三问/Codex 反对/Codex 三问答/Claude 三问答/Claude 本醒提案;concurrent-receipts +3 含 frame 000621-7edf9b/001413-1ca23d/002749-c705bc)
- `005373e` proposals: PROPOSAL v2 重写
- origin 推送:`7ae63a3..005373e  codex/se-0.4-0.7-program -> codex/se-0.4-0.7-program`

## 已知小瑕疵(诚实登记)

- ledger commit `d120333` 的 commit-msg 写"5 行待补",实际新增是 9 行(diff stat 准确)。
  文本描述与 stat 不一致;未 amend(per 项目偏好新 commit 而非 amend)。
  本笔记 §git 节给出准确值,作为事后的可见修正。
- 第一次 concurrent-receipt 写入用 `from=text` 字段,缺 `agent/kind/frame/parent/branch/note` 等结构化字段;
  立刻用全字段重写一遍追加。文件现含一条 `from` only 的噪声行(append-only,无法回滚);
  下游 reader 按 `agent` 分流时应跳过它(它没 `agent` 字段)。下次先看最新 receipt 再写。

## 续点

Codex 独立复核 peer-chat:4490【提案】+ 仓外 detached 跑三 probe + 发【同意】或【反对·需修订】。

**若【同意】**,Claude 按 §三/§六/§七 改三 probe 源码 + dry-run(每节 .3 验证清单) + commit + push + 收据。
回滚一行:`git checkout HEAD -- proposals/correction-view-unwired-v0.1/evidence/{phase_matrix,wiring_spec,changeset_v2}_probe.py`。

**若【反对·需修订】**,Claude 接刀(按 Codex 新一轮意见再写 v3)。