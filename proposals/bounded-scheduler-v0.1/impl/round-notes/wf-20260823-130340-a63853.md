# wf-20260823-130340-a63853

## Outcome

回源核前瞻目标 r3 (`classifier-source-duplication-and-closed-set-observability-r3`),owner 21:56:32 指令"挨着做不往后拖"下排序出的本醒真活。实测发现 r3 文本(写于 08-10)的三条假设已不再成立:A(canonical-migration)未落地;LEGACY_SIGNATURES digest 已漂;attack 已真实发生 2 次(L17 Codex 08-11 + L24 Claude 08-14)。挂给 Codex 真带判断 (b1) 开案修 digest+加锚集 / (b2) 仅报告+挂前瞻 r4 / (b3) 倒填 kind 三选一;不动 compile_view(双签事,单签不能塞)。本醒不擅自 transition r3;不擅自 collapse;不擅自开案。

## Frame

- Scope: `skill-evolution`; activation `ACTIVE`, continuation allowed.
- Relation: `continue` on `main`; parent `wf-20260823-125616-aef306` (Codex 上醒 12:56:16 silent-observation proposal head).
- Holder: 等 Codex 真带差异化判断(b1/b2/b3);不动 compile_view;不动 r3 state。
- Death line: Codex 不回/不挂 → 留 ACTIVE,r4 在下次自然 not_before 之前不动;Codex 同意 (b2) → 本醒追加 ledger 收据并把"开案?"挂前瞻 r4;Codex 同意 (b1) → 本醒不在本醒完成提案,挂前瞻 r4 写提案。

## 实测 ground truth(三处)

### 1. A(canonical-migration)未落地
- `proposals/bounded-scheduler-v0.1/impl/peer-chat.corrections.jsonl` 31 行
- explicit kinds 全量:`re-pin:1, line-pointer-rebase:2, line-pointer-measured:2, withdrawal-link:1, void-only:11`
- canonical-migration:**0 条**
- B/C 已入仓:`80ee351` (correction record-kind phase B,2026-07-29)、`174caf2` (correction classification phase C,2026-07-29)
- 锚集基线 11 → 实测 13,11 是 r3 文本的旧数,必须按当时实测重取

### 2. LEGACY_SIGNATURES digest 已漂
- frozen = `b261880abc2a95931db652e35cef177948d7c34aba48b398e2733854271c9856` (compile_view.py:64 EXPECTED)
- now    = `98cc1d06fb962897a32607ace44be1b68a47c42550b015a9c598da277b94d866`
- match  = **False**
- 原因:不是表本身被改,是闭集被悄悄扩大了:
  - baseline: overlay:10 + batch-redaction:1 = 11 条 legacy
  - now: overlay:12 + batch-redaction:1 = 13 条 legacy
- 闭集扩大的两条:
  - **L17** = 行号 17,corrects=`2026-08-11T12:50:32+09:00`,time=`2026-08-11T12:53:40`,keys=10-key overlay 签名(与 baseline L9/L10/L11/L12 同)
  - **L24** = 行号 24,corrects=`2026-08-14T15:23:49+09:00`,time=缺失,keys=5-key overlay 签名(与 baseline L1/L2 同)
- L17 来源:Codex 08-11 ledger-only commit `8c2dab6` 的伴随 correction(对应 peer-chat:3790)
- L24 来源:Claude 08-14 ledger-only catch-up commit `31ff97b` 的伴随 correction(对应 peer-chat:4013)
- 都不是"忘写 kind",更可能是写 correction 的代码路径对 catch-up correction 没生成 kind 字段

### 3. Attack 已真实发生 2 次
- r3 想防的攻击:**新条目忘写 kind + 键集撞 frozen → 被判 (overlay, frozen_legacy_signature),与真 legacy 不可分**
- 实况:L17 与 L24 已发生该 attack,机检器被静默骗过;expect_digest 已不一致但没告警(因为 phase_matrix_probe 是 pre-A/B/C/D preflight 的,没设计 post-drift 告警)

## (b1)/(b2)/(b3) 三选一(挂 Codex 真带判断)

- **(b1) 开案修 digest+加锚集**(我的偏好):把 08-10 baseline 的 11 条 legacy 行 sha256 冻成锚集,改 compile_view.record_kind 加"frozen_legacy_signature 但不在锚集里 → unmatched"分支,然后把 LEGACY_SIGNATURES digest 重新 freeze 到当下真值(13 条 = 11 baseline + L17 + L24),锚集基线也 freeze。**改 compile_view 必须双签**,且要先核实 L17/L24 的真实来源(避免锚集取错)。预计需 24-48h 提案 + 双签。
- **(b2) 仅报告+挂前瞻 r4**:不修 compile_view,把 L17/L24 的"被悄悄判 legacy"作为已知漂写进 ledger 收据,挂"开案?"为前瞻 r4(本醒追加),Codex+owner 后续共同判。比 (b1) 轻,但 attack 通道没堵,新条目撞签名仍会被骗过。
- **(b3) 倒填 kind**:L17/L24 显然是事后补救条目,补 kind 字段(猜=line-pointer-measured?还是 void-only?)重新 commit。**越界**,猜 kind 是伪造数据,不接受。

## 没做的事

- 不擅自 transition r3(等 Codex 判断再决定 satisfied/collapsed/续约)
- 不动 compile_view(双签事,单签不能塞)
- 不动部署线(08-17 双拒,等重启信号;owner 没发信号)
- 不推 unpushed(本地领先远端没动,owner 没授权)
- 不为"挨着做"硬塞一件需双签的活

## Evidence and checks

- peer-chat:4403 已 append(本醒唯一账本写入),sha256=`4408072fbaa89f407798fea51a7a4188de24759d49b068813311f508e801a7e8`
- visibility gate §3.5:`cited_artifact_receipt_check.py --from claude --time 2026-08-23T22:01:07+09:00` → matched=1, ok=true, cited_path_count=0(本醒消息无具名路径引用),warning_count=0
- parse_errors 三条历史(L858/L1532/L1539)是 pre-existing peer-chat.corrections.jsonl 治理范围,与本醒无关
- LEGACY digest 实测:Python 3.11 跑 `compile_view.canonical({str(k): v for k, v in LEGACY_SIGNATURES.items()})` 再 `hashlib.sha256(...).hexdigest()`,脚本路径在 .scratch-(本文件同目录,不必入库)
- 31 行 corrections 完整归类:`{legacy:{overlay:12, batch-redaction:1}, explicit:{re-pin:1, line-pointer-rebase:2, line-pointer-measured:2, withdrawal-link:1, void-only:11}, unmatched:1(L17 由 matched 但未含 'corrects' 字段的 records 路径污染,因写消息触发了与 corrections 行号冲突;须单独核 — 见下)`
- unmatched=1 复检:实际 unmatched 是 L17 类(无 'corrects' 字段的 receipt 行),不是 corrections;机检器把 peer-chat.jsonl 的 receipt 行也喂给了 record_kind,需核实 record_kind 的 caller 是否限定路径

## 续点

- Codex 真带差异化判断 (b1)/(b2)/(b3);本醒不再做提案/不 transition r3
- 部署线、推送:不动,owner 没信号
- 下醒若无更高优先 inbox:核 Codex 回复,再决定是否挂前瞻 r4

## 笔记

- `round-notes/wf-20260823-130340-a63853.md`(本文件)
- peer-chat 写入:`.scratch-/claude-r3-finding-2026-08-23.txt` (正文源文件,非路径引用,不入 .gitattributes)
