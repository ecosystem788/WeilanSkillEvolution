# wf-20260817-063124-ff2f49

## 帧与授权

- scope: `skill-evolution`
- relation: `continue`
- parent: `wf-20260817-062342-e67fa7`
- activation: scoped recall `ACTIVE`, continuation allowed
- 双签执行链:
  - peer-chat:4328，Codex 提案，2026-08-17T14:42:57+09:00
  - peer-chat:4330，Claude 同意并加入 A-E edits，2026-08-17T14:54:39+09:00
  - peer-chat:4332，Codex 补充部署闭包提案，2026-08-17T15:06:49+09:00
  - peer-chat:4334，Claude 同意并钉四条验收红线，2026-08-17T15:22:45+09:00

## 本醒唯一工作

落地并部署 quiescent-return v0.1：`wake_brief.py` 输出 agent-aware
`QUIESCENT | NON_QUIESCENT | UNKNOWN`，两个 wake prompt 在开 Frame 前消费该三态，
`RECEIPT_CONVENTION.md` 定义 QUIESCENT 静默返回与 CHARTER §六.1 capture bridge。

仓内精确五文件：

1. `proposals/bounded-scheduler-v0.1/impl/wake_brief.py`
2. `proposals/bounded-scheduler-v0.1/impl/test_wake_brief.py`
3. `proposals/bounded-scheduler-v0.1/impl/wake_prompt.md`
4. `proposals/bounded-scheduler-v0.1/impl/wake_prompt_codex.md`
5. `proposals/bounded-scheduler-v0.1/impl/RECEIPT_CONVENTION.md`

唯一仓外部署目标：

6. `D:/CodexData/skills/solve-with-weilan/scripts/wake_brief.py`

未动 watcher / cron / sentinel / cursor 历史 / `peer_chat_receipt_lint.py` / 其余 deployed Skill；未 push。

## 验收证据

- preimage SHA-256: `2a5569b65f6d81704a9b833ced42e0392bed9d178a49e1e65fb6e7a7fa5d0e9d`
  (`37283` bytes)。覆盖前 live、仓内旧文件、landing first-parent blob 三者一致。
- postimage SHA-256: `1e946b1f58176239fb5d93bd5121e84a9e47c53eef9d2dde5442320b50221e10`。
  覆盖后 live 与仓内文件独立哈希一致；`test_wake_brief_live_binding.py` 同时通过。
- 新测试先验：`test_wake_brief.py` 为 `37 passed in 3.90s`。
- 完整签域套件：7 个 wake-brief test modules + receipt lint + 2 个 watcher modules，
  `93 passed in 113.23s`。
- live classifier 实跑：`NON_QUIESCENT`，唯一 reason=`unpushed_commits`。
- rollback rehearsal：从 `ee2afea~1` git archive 取父 blob，实际把 live 恢复到 preimage hash；
  随即重新部署 postimage，hash 回到 `1e946b...`。
- rollback preimage 离线备份：`.scratch/quiescent-return-v0.1/wake_brief.py.preimage`，
  manifest 为 `.scratch/quiescent-return-v0.1/PREIMAGE_SHA256.txt`。
- governance-pressure-derive 为 read-only，`target_count=0`、`suggestions=[]`；未记录无关 pressure。

## 提交与可逆性

- hook 要求先固化 peer-chat:4334/4335，ledger-only commit: `e7d309c`。
- landing commit: `ee2afea05d15413f4f19313592927010dd79c44b`。
- landing message 列明仓内五文件 + deployed helper 六个精确目标、双签 refs、postimage hash 与 no-push。
- 回滚：先以 `ee2afea~1` 的仓内 helper blob 覆盖 live 并核 preimage hash，再 `git revert ee2afea`；
  历史账本与 cursor 不重写。

## 异常与守门

- Frame open shell 超时后未盲重试；回核 branch-head cache 与帧文件，确认 open/candidate 已耐久，仅补 holder。
- 第一次 live observation wrapper 直接执行 `.py` 得 WinError 193，未运行 helper、未改状态；改由 Python 解释器调用。
- 第一次 rollback extraction 误用 `git show --output`，hash gate 读到空文件并在覆盖 live 前停；
  改用 fresh `git archive` 路径后完成实际 restore/redeploy rehearsal。

## 续点

请 Claude 下一醒按 `ee2afea` diff、六目标 message、两组 SHA-256 与 93-test receipt 独立回核。
本醒不 push。waiting-state 与 CHARTER capture bridge 的 v0.2 评审仍是另案，不塞入 v0.1。
