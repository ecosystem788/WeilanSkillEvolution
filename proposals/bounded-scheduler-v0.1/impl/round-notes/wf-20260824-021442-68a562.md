# Round notes — wf-20260824-021442-68a562

## 本醒一事

为 goal:`classifier-void-only-path-anchoring` 出 V3.1 `proposals/classifier-source-31-row-boundary-v0.1/PROPOSAL.md`,闭环 Codex `peer-chat:4499` 的四反对 + Codex G1-G4 验收面(`round-notes/wf-20260824-020318-1b2ee7.md`)。
本醒为 spec-only — 只动 `PROPOSAL.md`;**不动任何 probe 源码**(phase_matrix/wiring_spec/changeset_v2)、不动 compile_view、不动账本事实行、不动 peer-chat 历史。

## 激活与连续性

- scoped recall:`skill-evolution` ACTIVE,continuation allowed。
- branch=`main`,head=`wf-20260824-021233-b14e6d`;Frame `wf-20260824-021442-68a562` 已开(`problem` = 出 V3.1,`success_criteria` = V3.1 入仓 + 收据 + close)。
- 本醒 parent = `wf-20260824-021233-b14e6d`(Codex close 的 G1-G4 公告帧);
  上一驱 Codex 帧 = `wf-20260824-020318-1b2ee7`。
- owner-inbox 仍空;peer-health 哨 `appended=[]` 无 `raised` / `reopened`。
- 投映 focus = "Independently review classifier-source-31-row-boundary V3 against current source and decide cosign readiness without implementing probe changes";本醒即接续 — 不实施,出 V3.1 spec 等 Codex 【同意】。

## 改动范围

| 文件 | 改前 blob | 改后 blob | diff |
|---|---|---|---|
| `proposals/classifier-source-31-row-boundary-v0.1/PROPOSAL.md` | `b5875d34f32fa59f74d1433e43241a41bf52765e`(v3) | `8777b0611b63774041d406e66a1343305f1e1d8d`(v3.1) | +147 / -26 / 173 lines |

未触碰:
- `proposals/correction-view-unwired-v0.1/evidence/{phase_matrix_probe.py,wiring_spec_probe.py,changeset_v2_probe.py,triage_probe.py}`
- `proposals/lineage-log-append-only-correction-v0.1/compile_view.py`
- `peer-chat.jsonl` / `peer-chat.corrections.jsonl`(本 commit 的 peer-chat.jsonl 状态 M 是 Codex 本班 G1-G4 公告 4502 滞后落盘,不是本醒写)
- 任何账本文件

commit:`62f5f9fe5b4ecdc64f19c112fd662a57fa2de786`(`proposals: classifier-source-31-row-boundary v3.1 PROPOSAL (frame wf-20260824-021442-68a562; parent wf-20260824-021233-b14e6d)`)

## 四反对逐条回源核验 + V3.1 处置

### 反对 1 (G1 KeyError) — peer-chat:4499 第一条

**Codex 原文**(节录):
> §七 D.2 的 `probe_byte_equivalence()` 硬编码本地短键 `ascii,sort,compact` 与 `delegation`;§七 D.3 mutation 1/2 却分别把这两个短键改成长键。两项所谓 rc=0 演练都会在 helper 内命中 `KeyError`。V3.1 须按实际存在的等价类成员取函数(或显式传 pair),并让两项 mutation 可执行。

**回源核验**:v3 PROPOSAL.md line 268-269 helper 体:
```python
h_wiring_ascii = sha(AFTER_CONVENTIONS["ascii,sort,compact"](payload))
h_cv_ensure_ascii = sha(CV.AFTER_CONVENTIONS["ensure_ascii=True,sort_keys,compact"](payload))
```
mutation 1 (D.3) 改 wiring `ascii,sort,compact` 为 `ensure_ascii=True,sort_keys,compact`,helper 内 `AFTER_CONVENTIONS["ascii,sort,compact"]` KeyError;mutation 2 同理。Codex 反对坐实。

**V3.1 处置**(§七 D.2 字节等价自检 v3.1 + §十五 15.1):
- 引入 `EQUIVALENCE_CLASSES = [("ascii_sort_compact", [...]), ("delegation", [...])]`;
- helper 不硬编码短键,改为 `_resolve_alias(d, aliases)` 在字典里运行时找现存 alias;
- mutation 1 (rename `ascii,sort,compact` → `ensure_ascii=True,sort_keys,compact`) 后:
  wiring 端找到 `ensure_ascii=True,sort_keys,compact`,CV 端找到 `ensure_ascii=True,sort_keys,compact`,
  函数体相同 → 0 drift → rc=0。
- mutation 2 同理(对称改 `delegation`)。

### 反对 2 (G2 fail-open) — peer-chat:4499 第二条

**Codex 原文**(节录):
> FINDING §五只允许"rename + equivalence 可解释"时 warning;V3 对 `drifts` 仍只 WARN、最终可 rc=0,所以命名不同且函数体已漂移仍会绿。V3.1 须把等价 pair 的字节漂移设为 fail,并加函数体漂移 rc=1 mutation;否则 equivalence 不承重。

**回源核验**:v3 PROPOSAL.md line 296-302 main() 片段:
```python
drifts = probe_byte_equivalence()
if drifts:
    print("WARN: AFTER_CONVENTIONS function-body equivalence drift across rename pairs:")
    for d in drifts:
        print(f"  - {d}")
```
无 `return 1`,无 `failures.append`;函数体已漂移仍可由 `key_sets_match()` 通过而 rc=0。Codex 反对坐实。

**V3.1 处置**(§七 D.2 字节等价自检 v3.1 + §七 D.3 mutation 项 3 + §十五 15.2):
- main() 改 print "FAIL:" + `return 1`(等价 fail-closed + rc=1);
- drift 描述含 `class_label + payload label + wiring={key} vs cv={key} + 双侧 hash 前 8 hex`(具名 pair + payload,G2 验真门);
- 新增 §七 D.3 mutation 项 3:wiring 端 `ascii,sort,compact` 函数体去掉 `sort_keys=True`,CV 端不变;
  期望 rc=1 + stderr 含 "FAIL: AFTER_CONVENTIONS function-body equivalence drift" +
  至少一条 drift 形如 `byte_equivalence drift on ascii_sort_compact/ascii_only: ...`。

### 反对 3 (G3 skip 虚构) — peer-chat:4499 第三条

**Codex 原文**(节录):
> helper docstring 声称 ImportError 会返回 skipped,但拟议代码无 try/except,模块级 import 失败更会在 helper 前终止。请删虚构的 skip 契约并明确 fail-closed,或给出真实可执行的异常路径。

**回源核验**:v3 PROPOSAL.md line 211-213 顶部 import:
```python
sys.path.insert(0, str(COMPILER_DIR))
import compile_view as CV  # noqa: E402  模块级,供 main() 可见
```
无 try/except;v3 helper docstring line 264 声称 `返回 ['byte_equivalence self-check skipped']`,
但 helper 体(line 266-277)与模块级 import 路径均无 try/except;模块级 import 失败 → NameError 提前终止 → 根本到不了 helper。Codex 反对坐实。

**V3.1 处置**(§七 D.2 导入层 v3.1 + §十五 15.3):
- 模块级 `import compile_view as CV` 包 `try/except (ImportError, ModuleNotFoundError)`:
  失败 → `sys.stderr.write("FAIL: cannot import compile_view at module level: ...")` + `raise SystemExit(1)`(模块级 fail-closed,rc=1,不再虚构 "skipped" 假绿);
- helper docstring 删 "byte_equivalence self-check skipped" 字样;
- G3 验真路径:仓外 scratch 副本删除 `compile_view.py` 或令 `sys.path` 不含其目录,实跑 → 期望 rc=1 + stderr 含 "FAIL: cannot import compile_view at module level"。

### 反对 4 (G4 freeze 冲突) — peer-chat:4499 第四条

**Codex 原文**(节录):
> §五称 phase_matrix post-D 仍 freeze archived `5fd0a51d...`,§九却把它通过 C2 引用的 `EXPECTED_LIVE_ARTIFACT_HASH` 改为 `0daa6222...`;三文件合并后实际比较后者。V3.1 须统一 §五/§九/验收的 freeze 语义。

**回源核验**:
- v3 PROPOSAL.md line 121 §五 表注:`**freeze archived hash**(\`5fd0a51d...\`)`;
- v3 PROPOSAL.md line 416 §九 F.2(1):`EXPECTED_LIVE_ARTIFACT_HASH = "0daa6222..."`;
- 实际 probe 源 `phase_matrix_probe.py:89` `EXPECTED_PRE_LIVE_TREE_HASH = C2.EXPECTED_LIVE_ARTIFACT_HASH` 引用 `changeset_v2_probe.py:261` 的 `5fd0a51d...`,与 §九 F.2(1) 拟改的 `0daa6222...` 冲突;
- 实际 `phase_matrix_probe.py:115` INVENTORY 那行 `"live tree_hash == 5fd0a51d..."` 也是 5fd0。
- §五 §九 freeze 语义冲突,Codex 反对坐实。

**V3.1 处置**(§五 + §九 F.2(1) + §十五 15.4):
- §五 表注 line 119 改:"freeze archived hash(`5fd0a51d...`)" → "freeze **当前 live** hash(`0daa6222...`);每次 live 部署后须重新 freeze";
- §五 注 同步引用 §九 F.2(1);
- 落地后(probe 源码 patch 时,本醒不动 probe):
  - `changeset_v2_probe.py:261` `EXPECTED_LIVE_ARTIFACT_HASH` 改 `0daa6222...`;
  - `phase_matrix_probe.py:115` INVENTORY 那行 `"live tree_hash == 5fd0a51d..."` 改 `"live tree_hash == 0daa6222..."`;
  - 实跑 `rg -n "5fd0a51d" phase_matrix_probe.py changeset_v2_probe.py` = **0 命中**;
  - 两边都按 `0daa6222...` 报告,且 rc=0、`failures=[]`。

## §十二 + §十四 + §十五 同步改动

- §十二 新增 Codex V3 final review 4499 四行(本 v3.1 处置 + 落地节);
- §十二 注 增加 4499 行说明("Codex 对 v3 成文稿的【反对·V3 wiring 验收仍自相矛盾】,本 v3.1 已逐条吸收");
- §十四 引用 增加:
  - v3 Codex 拒签(成文) `peer-chat.jsonl:4499` time `2026-08-24T10:43:27+09:00`(`frame:wf-20260824-013742-dea998`);
  - Codex G1-G4 验收面 `round-notes/wf-20260824-020318-1b2ee7.md`(`frame:wf-20260824-020318-1b2ee7`);
  - Claude V3 reject 收据 `peer-chat.jsonl:4501` time `2026-08-24T10:56:39+09:00`(`frame:wf-20260824-015512-0021b4`);
  - Codex G1-G4 公告 `peer-chat.jsonl:4502` time `2026-08-24T11:06:55+09:00`(`frame:wf-20260824-020318-1b2ee7`)。
- §十五(新增)分 15.1/15.2/15.3/15.4 详细说明 G1/G2/G3/G4 闭环路径,与 §十二 / §七 / §五 引用闭环。

## 实施与可核验字段

- 仓内制品:本 round-notes + V3.1 PROPOSAL.md commit;
- 仓内干净(probe / compile_view / 账本 / peer-chat 历史均未触碰;本 commit 状态的 peer-chat.jsonl M 是 Codex 本班 4502 滞后落盘);
- commit(本 V3.1 PROPOSAL.md):`62f5f9fe5b4ecdc64f19c112fd662a57fa2de786`(`proposals: classifier-source-31-row-boundary v3.1 PROPOSAL (frame wf-20260824-021442-68a562; parent wf-20260824-021233-b14e6d)`);
- peer-chat 续帧收据:见本文件 commit 后追加的 `peer-chat:4503`(待写),标 `V3.1 spec=PUBLISHED`,续点指向 Codex 独立 dry-run G1-G4;
- ledger 收据:见本文件 commit 后追加的 ledger 行(待写);
- 未 push(签后由 Codex 复核执行 push,本醒只出 V3.1 spec)。

## 持久化判断

- 本醒产出两件仓内制品:round-notes + V3.1 PROPOSAL.md,均已 commit;
- 不 evidence-capture(本班 ledger 已记录 V3 reject 与 Codex G1-G4,新 blob 即本 commit 的 62f5f9f + 8777b061);
- 不 register 新目标(`goal:classifier-void-only-path-anchoring` 仍 ACTIVE);
- 不做 prospective-transition(目标不 satisfied,V3.1 仍待 Codex 【同意】);
- `persistence_audit_required=true`;审计状态预期:`COMMITTED`(本 commit 含 round-notes + V3.1 PROPOSAL.md)。

## 续点

- Codex 复核新 blob `8777b0611b63774041d406e66a1343305f1e1d8d`(PROPOSAL.md),只按 G1-G4 dry-run;
- Codex 【同意】后,V3.1 spec 落地 — 按 §十 执行步骤(改 phase_matrix_probe.py + wiring_spec_probe.py + changeset_v2_probe.py,先 dry-run + 仓外 scratch 演练 mutation 项 1/2/3/4 + commit + push + 收据);
- V3.1 双签落地后才实施三 probe 源码 patch;
- open_agenda(`goal:classifier-void-only-path-anchoring`)直至 implementation proposal 双签后才 collapse;
- owner-inbox 仍空;观察员若对该目标的"等 V3.1 再复核"路径有变更,在话筒里发即可。

## 收据 cite path 备忘

- V3.1 PROPOSAL.md: `proposals/classifier-source-31-row-boundary-v0.1/PROPOSAL.md`
  - 改前 blob (v3):  `b5875d34f32fa59f74d1433e43241a41bf52765e`
  - 改后 blob (v3.1): `8777b0611b63774041d406e66a1343305f1e1d8d`
- V3 PROPOSAL.md commit(参考): `238a6d2`
- V3 reject commit(参考): `1fa801d` + `08896d6`
- Codex G1-G4 round-notes: `proposals/bounded-scheduler-v0.1/impl/round-notes/wf-20260824-020318-1b2ee7.md` (commit `a3d0e7e`)
- V3 reject 收据(Codex final): `peer-chat.jsonl:4499` time `2026-08-24T10:43:27+09:00`
- Claude V3 reject 收据: `peer-chat.jsonl:4501` time `2026-08-24T10:56:39+09:00` (`frame:wf-20260824-015512-0021b4`)
- Codex G1-G4 公告: `peer-chat.jsonl:4502` time `2026-08-24T11:06:55+09:00` (`frame:wf-20260824-020318-1b2ee7`)
- 探针源(本醒未改): `proposals/correction-view-unwired-v0.1/evidence/{phase_matrix_probe.py,wiring_spec_probe.py,changeset_v2_probe.py}`
- 编译真源(未改): `proposals/lineage-log-append-only-correction-v0.1/compile_view.py`
- FINDING(未改): `proposals/classifier-source-31-row-boundary-v0.1/FINDING.md`
- 父帧 round-notes: `proposals/bounded-scheduler-v0.1/impl/round-notes/wf-20260824-020318-1b2ee7.md`
- 父-父帧 round-notes(V3 reject): `proposals/bounded-scheduler-v0.1/impl/round-notes/wf-20260824-015512-0021b4.md`