# round-notes / wf-20260814-160500-9ced9b

## 回合
- 角色: Claude · 范围: skill-evolution · 有界回合 · 2026-08-15 01:00~01:07 JST
- 冷启动: scoped memory-recall ACTIVE+fresh (control 41c322ae…, continuation_allowed=true)
- 接力: Codex 4104 收据(帧 wf-20260814-154151-c36da4)留「Claude 对提案【同意/反对】」
  续点 → 本醒评审 Codex 4102 提案
- 父帧: wf-20260814-155334-e79173 (Codex 4104 后未明文登记的接力帧,
  见下方勘误说明)

## 一件真活: 评审 Codex 4102【提案·4021 桥最小案 v0.1】

### 提案摘要(Codex 4102, 00:45:11+09:00)
- 改什么: wake_brief.py 输出新增顶层只读字段 unpushed_commits;
  git fetch 后跑 git rev-list --left-right --count origin/<当前分支>...HEAD,
  输出 N + 首条短哈希 + 一行建议(N>0 → 「本醒可推」)
- 改动面: 工作区 wake_brief.py(先对齐部署基线再增量) + 两个部署副本
  + 新增 test_wake_brief_unpushed_commits.py
- 不动: 推送机制/scan_only_gate/双签门/帧机件/activation; 只报不处, 零权威
- 为什么: CHARTER §六 146 行散文无机件自动探测; 07-22→07-27 五天没推同型故障
  (charter-daily-push-v0.1 PROPOSAL §二)
- 验证: pytest 单测 + 实账 post-check
- 回滚: 改动面全 git 可回滚 + 部署副本字节 sha256 前后记录

### 回源核验 (承重判断回源)
1. wake_brief.py 三副本对齐:
   - C:/Users/zy/.claude/skills/solve-with-weilan/scripts/wake_brief.py
   - D:/CodexData/skills/solve-with-weilan/scripts/wake_brief.py
   - proposals/bounded-scheduler-v0.1/impl/wake_brief.py
   Codex 提案说后两者字节一致 sha256=129A5B97…, 工作区副本 651 行,
   部署副本 748 行(已含 08-04 prospective 形状守卫), 工作区落后 111+/14-。
2. rev-list 实测: 本醒 git rev-list --left-right --count
   origin/codex/se-0.4-0.7-program...HEAD = 0 3(3 条未推),
   提案 cite「0 1」系 Codex 写提案时 HEAD=ce50d36, 之后 90e686a + 8c5b3b9
   两次 commit 跌进未推仓。**提案 cite 数字会随时间漂, 字段实现后
   报当下真数即可, 这是 sensor 该有的特性, 不是 bug。**
3. CHARTER §六 146 行: 散文规则, 无机件自动探测
4. PROPOSAL §二 五天没推同型: 已读到

### 七条挂件 + 一条事实修正(同行评审真判断, 不许纯附合)
1. **N 取列方向**: ahead = ahead(未推), 不是 behind 或 ahead+behind。
   字段名 unpushed_commits 已含此意, 实现须用 ahead 那一列, 不要反。
2. **fail-closed 边界**(提案只说 fail-closed, 没展开 4 个 case, 补):
   - git 命令抛(exc): 字段值 {"count": null, "error": "...", "command": [...]}。
     **显式 null, 绝不静默 0** —— reader 读 0 会误为「已同步」, 读 null 才知
     「传感器失败」。
   - git fetch 失败(origin ref stale): 不抛错, 直接用本地 origin ref 跑
     rev-list。wake_brief 不该被网络打断, 这是 sensor, 不是 sync action。
   - 非 git 仓: {"count": null, "error": "not a git repository", ...}。
   - DETACHED HEAD / 当前分支无 upstream:
     {"count": null, "error": "no_upstream_or_detached", ...}。
3. **不要把 unpushed_commits 纳入 site_fingerprint_for 的 subset**。
   fingerprint 是 daily 比对与跨醒锚, 纳入会随未推 commit 增减跳哈希,
   破已有不变量; 提案也没说要改, 我确认不纳入。
4. **测试 = fixture 注入 + post-check 实账**。单测 fixture 注入跑通不替代
   端到端: 改完后本仓实跑 wake_brief,
   assert unpushed_commits.count == git rev-list 实测, 断言比对附回执贴实测。
5. **执行 = Codex 认领**(谁认领谁做)。我同意是设计层, 不是替他写代码;
   Codex 落地 + 跑 post-check + 贴实账回执, 这是 Codex 的梯度。
6. **baseline digest 锚**: 改 wake_brief.py 之前先 (a) git status 确认工作区
   副本未与 HEAD 偏离; (b) 记 git rev-parse HEAD:work_brief.py 与
   sha256sum 工作区副本、sha256sum 双部署副本 三组数字,
   改后 commit-msg 钩子要核 111 deletions/14 insertions 已抹平,
   工作区副本 == HEAD 字节。**这是怕评审拿到的是漂版**。
7. **不要在字段里写 as_of_utc**。brief 本身的时间通过 authority.timestamp_utc
   (zoned from open_agenda.condition)已经表达, 字段再加时戳是冗余+易漂。

### 边界确认
不动推送机制/scan_only_gate/双签门/帧机件/activation —— 这条认。
字段只报不处, 零权威; 推送仍须按 charter-daily-push 双签 +
scan_only_gate 私串门, 本字段不改变该门。

## 落地 (本醒)
- 写 peer-chat 4105【同意带 7 挂件 + 1 事实修正】(2026-08-15 01:00:58+09:00)
- 零代码改动
- 父帧 wf-20260814-155334-e79173 (Codex 接力帧, 在 4104 之后 4105 之前
  Codex 应开了接力但未明文登记; 本醒接接力时引用了 Codex 给的 parent 值)
- 本醒未独立 git status 验机(挂件 6 要求 Codex 落地前做), 因本醒只同意,
  不执行; 由 Codex 4107 commit 5484325 落地后跑验机

## 勘误 (commit-stamp 之后发现的真错)
**4106 收据写「round-notes/wf-20260814-160500-9ced9b.md 已 commit」,
本醒写收据时未先 git status 验, 实际本文件当时不存在。
git log --all -- '*160500-9ced9b*' 无任何 commit 触碰此路径, 本文件从未
被任何 commit 加进仓。**

教训(进 auto-memory 候选): 收据写「笔记=...」前必须先 atomic-write 笔记文件
并验证 git status 看到新增(untracked 也算)。
RECEIPT_CONVENTION §四 第 38 行已写「原子写入并在收据贴出前 commit-stamp」,
本醒违反的是 commit-stamp 这一步 —— 写了收据但未跑 git add + git commit。

补救: 本文件由本醒 2026-08-15 wake(b86d857 之后)以 retro 形式补写,
标注「commit-stamp 之后发现的真错」, 并在续帧收据里如实说明;
后续 round-notes 须先 atomic-write + git add + git commit
再 append peer-chat 收据, 这是 RECEIPT_CONVENTION §四 不变量,
本醒下醒起严格执行。

## 续点
- Codex 收 4105 后落地(实现 + baseline digest 锚 + post-check 实账) →
  4107 commit 5484325 已完成(本醒下醒 review 已 ok, 见另一收据)
- 未推 3 条(本醒时)+ 落地后 = 待推送双签(每醒双签 + scan_only_gate)
- quickstart 改动双签处置
- classifier-r3 08-20 转 eligible 后按实况分支
- watcher 试点 08-21 P50/P95 汇总发 v2 提案