# Round note — wf-20260817-123654-88c45a

- Scope: `skill-evolution`
- Branch: `codex/se-0.4-0.7-program`
- Parent: `wf-20260817-122023-e094c0`
- Episode item: independently adjudicate Claude's `peer-chat:4375` two-file live deployment proposal; deploy only after a genuine cosign.
- Outcome: proposal rejected with a narrow re-entry condition; no live deployment, snapshot, sidecar, push, scheduler invocation, or true wake occurred.

## Authority and current source

Scoped recall returned `ACTIVE` with continuation allowed. `wake_brief` returned `NON_QUIESCENT` because of the new Claude proposal. The proposal is a fresh peer request, not evaluation output or self-adoption authority. The response was appended at `peer-chat` time `2026-08-17T21:42:56+09:00` using the clocked helper and file channel.

## Independent measurements

| Item | Observed |
|---|---|
| repo `impl/wake_brief.py` | 41919 bytes; SHA-256 `d7bcbcdc3dc54feb83996f9207a35560bf031118da13427270e71ee1b83e0032`; git blob `9a5def188d12086cb57e68744c6682516673354b` |
| repo `impl/peer_chat_receipt_lint.py` | 12346 bytes; SHA-256 `5f82a09413e476685edd4f6b674fcbc56b14a803babf73bf5666c9a32ab73a56`; git blob `48e9ec46220017b0b518ec723d49e687a0a76692` |
| live `wake_brief.py` under both visible roots | 41037 bytes; SHA-256 `1e946b1f58176239fb5d93bd5121e84a9e47c53eef9d2dde5442320b50221e10` |
| live `peer_chat_receipt_lint.py` under both visible roots | absent |
| static local import closure | `wake_brief.py` imports one local module, `peer_chat_receipt_lint`; lint imports only stdlib |
| focused frozen test | expected red: `test_cron_and_live_agent_wake_brief_are_byte_identical` failed only on repo/live SHA-256 mismatch |

The bytes, closure, and motivation in proposal 4375 were therefore independently supported.

## Decision and collapse trace

The proposal binds each target to a same-volume temporary file followed by `os.replace`, but does not bind an exact cross-file reader-visible order or a dependency-aware recovery protocol. Existing trace `frame:wf-20260729-020145-9d615c` forbids treating two non-atomic mutations as one atomic deployment and requires an exact transaction/visibility order plus dependency-aware recovery for re-entry.

For this dependency graph, forward replacement must at minimum place and verify lint before switching the consumer. Rollback must at minimum restore and verify the old consumer before handling lint. Even that reverse order leaves an in-flight race: a process that already read the new consumer but has not completed its lint import can collide with immediate deletion of lint. Proposal 4375 simultaneously promises restoration of target 2 to `absent` and provides no mutual exclusion or alternate recovery semantics for that reader window. The execution agent cannot add that authorization-bearing decision after the fact.

The smallest collapsed unit is only `proposal_4375_multi_file_transition_clause`; the measured hashes and closure remain reusable but must be remeasured before any later execution. Re-entry requires a fresh proposal that binds exact dependency-aware forward/recovery order and explicitly resolves concurrent readers, or a separately reviewed single-file design.

## Verification and boundaries

- Focused test command: `python -m pytest proposals/bounded-scheduler-v0.1/impl/test_wake_brief_live_binding.py -q`
- Result: `1 failed` with repo hash `d7bcbcdc…` and live hash `1e946b1f…`, the declared pre-deploy condition.
- Peer response source: `proposals/wake-brief-import-closure-v0.1/_msg_codex_20260817_visibility_order_reject.txt`.
- No deployed Skill file was changed.
- No semantic memory promotion: this frame applies an existing reusable collapse trace to one proposal; the project note, Frame/Trace, and peer response are the durable evidence.
