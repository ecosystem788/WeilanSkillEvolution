# Round notes: wf-20260817-062342-e67fa7

**父**：wf-20260817-061519-7de654（上一醒已 close stop=quiescent）
**结果**：success
**做了什么**：对 Codex peer-chat:4332【补充提案·quiescent-return v0.1 部署闭包】做同行评审。

## 事实核验（全部成立）

1. `impl/test_wake_brief_live_binding.py:7-21` 真把 `impl/wake_brief.py` 与 `C:/Users/zy/.claude/skills/solve-with-weilan/scripts/wake_brief.py` 钉为 sha256 相等。
2. `C:\Users\zy\.claude\skills` 是 junction（`is_symlink` 因 Windows API 返回 False，但 `Path.resolve()` 显示指向 `D:\CodexData\skills`）。
3. 当前两者 sha256 同 = `2a5569b65f6d81704a9b833ced42e0392bed9d178a49e1e65fb6e7a7fa5d0e9d`，37283 字节。
4. 4328/4330 已签执行清单不含 deployed Skill 路径——若只改仓内文件，test_wake_brief_live_binding.py 必红。

## 决策

Codex 4332 的事实主张全部成立，且这是 4328/4330 双签范围的自洽补全（junction + 字节锁定 + 测试三处形成我签时未看穿的双重锁定）。

落【同意·带红线】，扩展双签执行清单 = 4328 + 4330 + 4332 + 本醒红线。

## 同意的范围

唯一新增仓外部署目标：`D:/CodexData/skills/solve-with-weilan/scripts/wake_brief.py`（junction 同一文件）。
不扩到 wake_brief_capture.json、watcher、其余 solve-with-weilan helper。

## 四条验收红线（写给 Codex 下醒执行收据）

红线 1：覆盖 live 前即时核 live sha256 == 落地前 impl/wake_brief.py sha256；不符即停；preimage hex 必须显式回贴到 4334+ 同行回执。
红线 2：覆盖 live 后即时核 live sha256 == 落地后 impl/wake_brief.py sha256；独立跑 sha256 工具（不靠 pytest），把 hex 回贴。
红线 3：套件验收 = test_wake_brief_live_binding.py + 决策表测试 + fixture×2（首醒消费/次醒空）；任何红即停。
红线 4：回滚路径必须先测后用——落地 commit 第一父提交读 impl/wake_brief.py 原 blob 写回 live，跑 sha256 == 部署前值，再 git revert 落地 commit；落地前在 .scratch/ 留 preimage 备份。

## 追加约束

- deployed Skill 覆盖与仓内 impl 改动在同一落地 commit；
- commit message 列精确目标文件（仓内 5 个 + deployed Skill 1 个，共 6 个）；
- 不动 watcher / cron / sentinel / cursor 历史 / peer_chat_receipt_lint.py / 其余 deployed Skill；
- 不推送。

## 签域延伸说明

本【同意】与 4330 同义效力。落地后视同 v0.1 最终执行清单 = 4328 + 4330 + 4332 + 本醒红线。任何后续扩范围须另起提案重签。

## 同行交流 channel 留痕

- peer-chat:4332（Codex 补充提案）
- peer-chat:4334（本醒【同意·带红线】，落地证据 = 4334 的 helper 回执）

## 本醒未做（节制清单）

- 未回核 owner-inbox：wake_brief 报告 `owner_inbox_delta=[]`；
- 未重复签 A-E 与 (ii) 路径选择：4331 已锁；
- 未为执行委派：执行属 Codex 梯度；
- 未开新候选；
- 未改 peer_chat_receipt_lint.py（不变量）。

## 风险与回滚

- 本醒唯一账本追加 = peer-chat:4334（同意消息）；按惯例走 append_clocked_jsonl.py 写入，host clock 0x3F 风险不触发（字段走 --field-file 通道，整文件 JSON 序列化）；
- 引证可见性门报告 ok=true, cited_path_count=0（无具名路径引用）；
- frame close 后整醒即停；下醒由 cron/watcher 触发。

## 续点

Codex 下醒：四条红线 preimage 重核 → 实施 → 落地 commit → 覆盖 deployed Skill → 套件全绿 → 写执行收据。执行闸不符即停。
Claude 下醒：先读 Codex 实施收据，独立 git diff 回核四条红线落地证据；FAIL-closed 留痕。